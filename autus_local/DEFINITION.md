# AUTUS Definition v1.0 Final Lock

**문서 상태**: 🔒 LOCKED  
**최종 수정**: 2025-12-26  
**버전**: 1.0.0  

---

## 1. 한 문장 정의

### Internal Definition (개발용)
> AUTUS는 의미론적으로 중립적인 로컬 전용 물리 시뮬레이터로서, 6차원 상태 벡터를 관측하고 사용자의 선택에 따른 물리적 결과를 예측하되, 판단·추천·최적화를 수행하지 않는다.

### External Definition (공개용)
> AUTUS는 로컬에서 작동하는 상태 관측 시스템이다.

---

## 2. Hard-Lock 제약조건

### 2.1 No Egress (명확한 범위)

```
┌─────────────────────────────────────────┐
│  NO EGRESS POLICY                       │
├─────────────────────────────────────────┤
│  Outbound HTTP/WS calls      ❌ 금지    │
│  Telemetry / Analytics       ❌ 금지    │
│  External DB / Cloud Sync    ❌ 금지    │
│  External API calls          ❌ 금지    │
│  Local loopback (127.0.0.1)  ✅ 허용    │
│  Local file I/O              ✅ 허용    │
│  Local SQLite                ✅ 허용    │
└─────────────────────────────────────────┘
```

### 2.2 Privacy & Anonymity

| 항목 | 상태 |
|------|------|
| Real names | ❌ 금지 |
| External IDs | ❌ 금지 |
| Location tracking | ❌ 금지 |
| Device fingerprinting | ❌ 금지 |
| Local pseudonym only | ✅ 허용 |

### 2.3 Deterministic Execution

```
동일 입력 → 동일 출력 (항상)
θ 파라미터 노출: ❌ 금지
랜덤 시드 고정: ✅ 필수
```

### 2.4 Cost-Only CU

```
Reward / Gamification    ❌ 금지
Ranking / Leaderboard    ❌ 금지
Recommendation           ❌ 금지
Nudge / Push             ❌ 금지
Cost display only        ✅ 허용
```

---

## 3. State Vector Architecture

### 3.1 Core State (6축 - Hidden Layer)

내부 물리 커널에서 사용하는 완전한 상태 벡터:

```
S_core = [
  stability,    // 구조적 안정성
  pressure,     // 외부/내부 압력
  drag,         // 저항/마찰
  momentum,     // 운동량/관성
  volatility,   // 변동성
  recovery      // 회복력
]
```

**단위**: 모든 축은 무차원 정규화 값 [0, 1]

### 3.2 Displayed State (3축 - UI Layer)

사용자에게 표시되는 요약 벡터:

```
S_display = [E, F, R]

E (Energy)  = f(stability, recovery)
F (Flow)    = f(momentum, drag)
R (Risk)    = f(pressure, volatility)
```

**변환 함수**:
```python
E = 0.6 * stability + 0.4 * recovery
F = 0.7 * momentum - 0.3 * drag
R = 0.5 * pressure + 0.5 * volatility
```

### 3.3 계층 관계

```
┌─────────────────────────────────────────┐
│  UI LAYER (User sees)                   │
│  ┌─────┐ ┌─────┐ ┌─────┐               │
│  │  E  │ │  F  │ │  R  │               │
│  └──┬──┘ └──┬──┘ └──┬──┘               │
│     │       │       │                   │
│  ═══╪═══════╪═══════╪═══════════════   │
│     │       │       │                   │
│  ┌──┴───────┴───────┴──────────────┐   │
│  │  PHYSICS KERNEL (6-axis)        │   │
│  │  stability│pressure│drag        │   │
│  │  momentum │volatility│recovery  │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 4. Reference Anchor (기존 Goal Anchor 수정)

### 4.1 명칭 변경

| 기존 | 수정 |
|------|------|
| Goal Anchor | Reference Anchor |
| Goal | Reference Point |
| S* | S_ref |

### 4.2 정의

```
Reference Anchor S_ref
├── Optimization target    ❌ 아님
├── Evaluation standard    ❌ 아님
├── Ideal state            ❌ 아님
└── User-defined reference ✅ 맞음
```

**용도**: ΔS 크기 계산의 기준점으로만 사용

```python
ΔS = S_current - S_ref  # 방향과 크기만 표시, 좋고 나쁨 아님
```

### 4.3 UI 표현

```
┌─────────────────────────────────────────┐
│  REFERENCE ANCHOR                       │
│  ┌───────────────────────────────────┐  │
│  │  E: ████████████░░░░░░░░  65      │  │
│  │  F: ████████░░░░░░░░░░░░  48      │  │
│  │  R: █████░░░░░░░░░░░░░░░  32      │  │
│  └───────────────────────────────────┘  │
│                                         │
│  NOT A GOAL. JUST A REFERENCE POINT.    │
└─────────────────────────────────────────┘
```

---

## 5. Semantic Neutrality (의미론적 중립성)

### 5.1 핵심 원칙

```
시스템은:
├── 관측한다     ✅
├── 예측한다     ✅ (물리적 결과만)
├── 표시한다     ✅ (수치/파형만)
├── 판단한다     ❌
├── 추천한다     ❌
├── 조언한다     ❌
└── 최적화한다   ❌
```

### 5.2 용어 정책

| 금지 용어 | 대체 용어 |
|-----------|-----------|
| Loss Velocity | ΔS/Δt (State Divergence Rate) |
| Loss | Deviation |
| Goal | Reference |
| Optimal | - (사용 금지) |
| Best | - (사용 금지) |
| Recommend | - (사용 금지) |
| Should | - (사용 금지) |

### 5.3 UI 표현 규칙

```
허용:
├── 숫자 (수치)
├── 파형 (그래프)
├── 색상 (방향 표시: 청록/빨강)
├── 크기 (물리량)
└── 진동 (변화율)

금지:
├── "좋습니다" / "나쁩니다"
├── "추천합니다"
├── "휴식이 필요합니다"
├── "성공" / "실패"
└── 모든 가치 판단 텍스트
```

---

## 6. Physics Map Architecture

### 6.1 수정된 구조

```
Physics Map
├── Nodes: Person (사람만)
├── Motion: CU (Cost Units = 돈)
├── Dimension: Time
└── Fields (오버레이):
    ├── Pressure Field
    ├── Flow Field
    └── Authority Field (기존 Approval → Field로 격하)
```

### 6.2 Approval 처리

**기존**: APPROVAL을 독립 노드로 표시  
**수정**: Authority Field (오버레이)로 격하

```
Authority Field
├── 독립 노드 ❌
├── 연결선 속성 ✅
└── 배경 왜곡 필드 ✅
```

### 6.3 시각화

```
┌─────────────────────────────────────────┐
│                                         │
│        ○────────○────────○              │
│       ╱│╲      ╱│╲      ╱│╲             │
│      ○─┼─○────○─┼─○────○─┼─○            │
│       ╲│╱      ╲│╱      ╲│╱             │
│        ◉────────○────────○              │
│       SELF                              │
│                                         │
│  Nodes: Person only                     │
│  Lines: CU flow (thickness = magnitude) │
│  Field: Authority (background warp)     │
└─────────────────────────────────────────┘
```

---

## 7. 8-Stage Loop

### 7.1 단계 정의

```
1. REALITY   → 현실 입력 (센서)
2. STATE     → 상태 측정 (6축 → 3축)
3. THRESHOLD → 임계값 확인
4. FORECAST  → 물리적 결과 예측 [RISK STAGE]
5. DECISION  → 사용자 결정 [RISK STAGE]
6. ACTION    → 실행
7. LOG       → 불변 기록
8. LOOP      → 다음 사이클
```

### 7.2 DECISION 단계 명확화

```
DECISION Stage
├── System decides    ❌ 아님
├── User decides      ✅ 맞음
├── System role:
│   └── 예측된 물리적 결과만 표시
└── User role:
    └── 최종 선택 및 실행
```

### 7.3 시각화

```
        REALITY
           │
    ┌──────┼──────┐
    │      ▼      │
  LOOP ← STATE    │
    │      │      │
    │      ▼      │
    │  THRESHOLD  │
    │      │      │
    │      ▼      │
    │  FORECAST   │  ← Risk Stage (Red)
    │      │      │
    │      ▼      │
    │  DECISION   │  ← Risk Stage (Red) / USER DECIDES
    │      │      │
    │      ▼      │
    │   ACTION    │
    │      │      │
    │      ▼      │
    └──── LOG ────┘
```

---

## 8. Justice Mechanism

### 8.1 핵심 규칙

```
2/3 Rule:
├── 상위 1/3 손해 없이 하위 2/3 이득 ❌ 금지
├── 특정 노드 과잉 손실 (>2σ) ❌ 제한
└── 양자 터널링으로 우회 ❌ 차단
```

### 8.2 임계값 테이블

| 메트릭 | 임계값 | 제약 |
|--------|--------|------|
| Max Single Loss | >30% | OR +50% |
| Gini Coefficient | >0.7 | 선택지 제한 |
| Contagion Risk | >0.5 | 쿨다운 강제 |
| Cascade Depth | >3 | 차단 |

### 8.3 UI 표현 규칙 (중요)

```
Justice 발동 시:

금지 표현:
├── "Justice Activated"
├── "Constraint Applied"
├── "Warning"
└── 모든 명시적 알림

허용 표현:
├── OR (Overdose Risk) 수치 상승
├── 선택지 수 감소 (시각적)
├── 회전 속도 감소 (암시적)
└── 쿨다운 타이머만 표시
```

---

## 9. 4-Layer Architecture

### 9.1 레이어 구조

```
Layer 1: CORE LOOP (항상 보임)
├── 회전 루프 + 8단계
├── Reference Anchor (E/F/R)
├── ΔS/Δt 표시
└── AUTO 버튼

Layer 2: SENSORY INPUT (↑ 스와이프)
├── 카메라 / 마이크 / 활동 / 수화
├── Raw → Vector 변환
└── Raw 데이터 즉시 폐기

Layer 3: PHYSICS MAP (← 스와이프)
├── Person 노드만
├── CU 흐름선
└── Authority Field (오버레이)

Layer 4: FEEDBACK (↓ 스와이프)
├── ΔS/Δt 표시
├── 도파민/스트레스 파형
└── ε 오차 그래프
```

### 9.2 상호작용

```
제스처:
├── ↑ 스와이프 → Sensory Layer
├── ← 스와이프 → Physics Map
├── ↓ 스와이프 → Feedback Layer
└── 반대 방향 → 레이어 닫기

키보드:
├── Space → AUTO 토글
├── ArrowUp/Down/Left → 레이어 열기
└── Esc → 레이어 닫기
```

---

## 10. Design Language

### 10.1 색상 체계

| 색상 | Hex | 의미 |
|------|-----|------|
| Teal | #00d4aa | 양(+) 방향 |
| Red | #ff3b5c | 음(-) 방향 |
| Blue | #00b4d8 | Flow/보조 |
| Black | #000000 | 배경 |

**중요**: 색상은 "좋음/나쁨"이 아닌 "방향"만 표시

### 10.2 텍스트 최소화

```
허용:
├── 축 레이블 (E, F, R)
├── 수치
├── 단위 (PER SECOND)
└── 단계명 (STATE, FORECAST, etc.)

금지:
├── 설명 문장
├── 조언/추천
├── 감정 표현
└── 가치 판단
```

### 10.3 홀로그램 효과

```
├── 투명도: 60-80%
├── Glow: 청록 산란
├── Depth: z축 레이어
├── Particles: 미세 입자
└── Reflection: 바닥 반사
```

---

## 11. Forbidden Operations

### 11.1 시스템 레벨

```python
FORBIDDEN_OPERATIONS = [
    "optimize",
    "recommend", 
    "rank",
    "score",
    "evaluate",
    "judge",
    "compare_users",
    "external_call",
    "track_location",
    "store_identity"
]
```

### 11.2 UI 레벨

```python
FORBIDDEN_UI_ELEMENTS = [
    "success_message",
    "failure_message",
    "recommendation_panel",
    "ranking_board",
    "achievement_badge",
    "notification_push",
    "share_button",
    "external_link"
]
```

---

## 12. Verification Checklist

### 12.1 Hard Constraints

| 항목 | 상태 |
|------|------|
| No Egress | ✅ |
| Local Only | ✅ |
| No θ Exposure | ✅ |
| Deterministic | ✅ |
| No Recommendation | ✅ |
| No Ranking | ✅ |
| No Judgment | ✅ |

### 12.2 Architecture

| 항목 | 상태 |
|------|------|
| 6축 Core State | ✅ |
| 3축 Display State | ✅ |
| Reference Anchor (not Goal) | ✅ |
| Person-only Nodes | ✅ |
| Authority as Field | ✅ |
| User DECISION | ✅ |

### 12.3 UI/UX

| 항목 | 상태 |
|------|------|
| One Screen | ✅ |
| Semantic Neutrality | ✅ |
| No Value Text | ✅ |
| Physics Only | ✅ |
| Holographic Design | ✅ |

---

## 13. Version History

| 버전 | 날짜 | 변경 사항 |
|------|------|-----------|
| 0.9 | 2025-12-25 | 초안 |
| 1.0 | 2025-12-26 | 검토 반영 Final Lock |

### 1.0 주요 수정 사항

1. ✅ State Vector 3축 ↔ 6축 관계 명확화
2. ✅ Loss Velocity → ΔS/Δt 용어 변경
3. ✅ APPROVAL 노드 → Authority Field 격하
4. ✅ Goal Anchor → Reference Anchor 명칭 변경
5. ✅ No Egress 범위 명문화
6. ✅ DECISION 단계 사용자 주체성 명시
7. ✅ Justice UI 표현 방식 제한

---

## 14. 서명

```
Document: AUTUS Definition v1.0 Final Lock
Status: 🔒 LOCKED
Author: AUTUS Core Team
Date: 2025-12-26

이 문서는 AUTUS의 기준 정의서입니다.
모든 구현은 이 문서를 따라야 합니다.
```

---

**END OF DOCUMENT**







