# 🔄 AUTUS n8n 워크플로우

AUTUS 신경망의 외부 데이터 혈관입니다.

## 📁 워크플로우 목록

| 파일 | 설명 | 주기 |
|------|------|------|
| `erp_to_autus_engine.json` | 학원 ERP → Physics v2.2 → 알림 | 6시간마다 |
| `weekly_v_report.json` | 주간 V 리포트 생성 + 카카오 발송 | 매주 월요일 9시 |
| `consensus_auto_standard.json` | 표준 솔루션 자동 등록 + Gate-2 경고 | 매월 1일 |

## 🚀 Import 방법

1. n8n Cloud 접속
2. **Settings > Import from File** 또는 빈 캔버스에서 `Ctrl+V`
3. 환경 변수 설정 후 **Activate**

## 🔐 필요 환경 변수

n8n **Settings > Variables**에서 설정:

```
# AUTUS API
VERCEL_API_URL=https://your-vercel-app.vercel.app
AUTUS_USER_ID=your-user-uuid

# Supabase (직접 연결용)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# 학원나라 ERP
HAGNARA_KEY=your-hagnara-api-key

# 알리고 (카카오 알림톡)
ALIGO_API_KEY=your-aligo-api-key
ALIGO_USER_ID=your-aligo-user-id
ALIGO_SENDER_KEY=your-aligo-sender-key
SENDER_PHONE=0212345678
ADMIN_PHONE=01012345678
```

## 📊 워크플로우 상세

### 1. ERP → AUTUS Engine (`erp_to_autus_engine.json`)

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  6시간마다 스캔  │───▶│  ERP 데이터 수집  │───▶│ Physics v2.2 계산│
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                      │
                       ┌─────────────────┐           ▼
                       │ Supabase 동기화  │◀──────────┘
                       └─────────────────┘
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
       ┌─────────────┐                 ┌─────────────┐
       │  위험 감지?  │                 │  기회 감지?  │
       └─────────────┘                 └─────────────┘
              │ Yes                          │ Yes
              ▼                              ▼
       ┌─────────────┐                 ┌─────────────┐
       │AI 보상 카드  │                 │  보상 배지   │
       └─────────────┘                 └─────────────┘
              │
              ▼
       ┌─────────────┐
       │ 카카오 알림톡 │
       └─────────────┘
```

**Physics v2.2 계산 로직:**
- `M (Mint)` = 수업료 × (1 + 성취도)
- `T (Tax)` = 미납액 + 관리비용
- `s (Synergy)` = 출석률 × 0.3
- `Entropy` = 미납(0.4) + 결석(0.3) + 성적하락(0.2)
- `V` = (M - T) × (1 + s)^t

### 2. Weekly V Report (`weekly_v_report.json`)

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ 매주 월요일 9시  │───▶│ 물리 상태 조회   │───▶│  리포트 생성    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                      │
                       ┌──────────────────────────────┴───────┐
                       ▼                                      ▼
                ┌─────────────┐                        ┌─────────────┐
                │ 카카오 발송  │                        │ 리포트 저장  │
                └─────────────┘                        └─────────────┘
```

**리포트 내용:**
- 총 V 합계
- 상태별 분포 (urgent/warning/stable/opportunity)
- 성장 TOP 3
- 위험 TOP 3 + 추천 Impulse

### 3. Consensus Auto Standard (`consensus_auto_standard.json`)

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  매월 1일 자정   │───▶│ 솔루션 랭킹 조회 │───▶│ 표준 자격 검사  │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                      │
                       ┌──────────────────────────────┴───────┐
                       ▼                                      ▼
                ┌─────────────┐                        ┌─────────────┐
                │신규 표준 있음?│                        │Gate-2 경고? │
                └─────────────┘                        └─────────────┘
                      │ Yes                                 │ Yes
                      ▼                                     ▼
                ┌─────────────┐                        ┌─────────────┐
                │  표준 등록   │                        │ 경고 발송   │
                └─────────────┘                        └─────────────┘
```

**표준 자격 기준:**
- 실효성 ≥ 80%
- 사용 횟수 ≥ 50회
- 평균 V 성장률 ≥ 15%

## 🧪 테스트 데이터

`erp_to_autus_engine.json` 테스트용 더미 데이터:

```json
{
  "students": [
    {
      "student_id": "STU001",
      "student_name": "김민준",
      "monthly_fee": 350000,
      "payment_status": "unpaid",
      "unpaid_balance": 350000,
      "attendance_rate": 65,
      "achievement_score": 0.6,
      "grade_trend": "down",
      "complaint_count": 2
    },
    {
      "student_id": "STU002",
      "student_name": "이서연",
      "monthly_fee": 350000,
      "payment_status": "paid",
      "unpaid_balance": 0,
      "attendance_rate": 98,
      "achievement_score": 0.9,
      "grade_trend": "up",
      "complaint_count": 0
    }
  ]
}
```

예상 결과:
- 김민준: `entropy=0.9` (urgent) → 카카오 알림 + AI 보상 카드
- 이서연: `status=opportunity` → 보상 배지 발급

---

*"데이터가 흐르면 가치가 쌓인다" - AUTUS*
