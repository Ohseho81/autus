# ═══════════════════════════════════════════════════════════════════════════════
# AUTUS Ingress (AWS ALB / Nginx)
# ═══════════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: autus-ingress
  namespace: autus
  annotations:
    # AWS ALB Ingress Controller
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
    alb.ingress.kubernetes.io/ssl-redirect: "443"
    alb.ingress.kubernetes.io/certificate-arn: ${ACM_CERTIFICATE_ARN}
    # Health check
    alb.ingress.kubernetes.io/healthcheck-path: /health
    alb.ingress.kubernetes.io/healthcheck-interval-seconds: "30"
    # WebSocket support for Socket.io
    alb.ingress.kubernetes.io/target-group-attributes: stickiness.enabled=true,stickiness.lb_cookie.duration_seconds=3600
spec:
  rules:
  - host: api.autus.ai
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: autus-backend-service
            port:
              number: 80
      - path: /socket.io
        pathType: Prefix
        backend:
          service:
            name: autus-backend-service
            port:
              number: 3001
  - host: autus.ai
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: autus-frontend-service
            port:
              number: 80
---
# Alternative: Nginx Ingress
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: autus-ingress-nginx
#   namespace: autus
#   annotations:
#     kubernetes.io/ingress.class: nginx
#     nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
#     nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
#     nginx.ingress.kubernetes.io/websocket-services: "autus-backend-service"
#     cert-manager.io/cluster-issuer: "letsencrypt-prod"
# spec:
#   tls:
#   - hosts:
#     - autus.ai
#     - api.autus.ai
#     secretName: autus-tls
#   rules:
#   - host: api.autus.ai
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: autus-backend-service
#             port:
#               number: 80
