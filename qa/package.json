{
  "name": "autus-visual-qa",
  "version": "1.0.0",
  "description": "AUTUS Visual QA — Golden Set + Diff Report",
  "type": "module",
  "scripts": {
    "capture": "npx tsx scripts/capture-golden.ts",
    "diff": "npx tsx scripts/diff-report.ts",
    "test": "npm run capture && npm run diff",
    "ci": "npm run diff"
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
