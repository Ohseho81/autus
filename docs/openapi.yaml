paths:
  /api/v1/risks:
    get:
      summary: "Get all risks"
      responses:
        200:
          description: "A list of risks"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Risk"
