# AUTUS 데이터 융합 아키텍처

> "모든 데이터는 V = (M - T) × (1 + s)^t 로 수렴한다"

---

## 🎯 핵심 원칙

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   다양한 데이터 → 단일 물리 공식 → 하나의 V 값              │
│                                                             │
│   "복잡한 현실을 단순한 숫자로 변환"                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 데이터 융합 흐름도

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        AUTUS 데이터 융합 파이프라인                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [Layer 1: 원시 데이터 수집]                                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ 학원 ERP │ │ 결제시스템│ │ 경쟁사API│ │ 커뮤니티 │ │ 정책뉴스 │          │
│  │ (학원나라)│ │ (토스)   │ │ (네이버) │ │ (학원노) │ │ (교육부) │          │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘          │
│       │            │            │            │            │                │
│       ▼            ▼            ▼            ▼            ▼                │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │                    [Layer 2: n8n 데이터 허브]                    │       │
│  │  웹훅 수신 → 정규화 → 필터링 → 라벨링 → Supabase 저장          │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │                  [Layer 3: Supabase 데이터 레이크]               │       │
│  │                                                                  │       │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │       │
│  │  │organisms│ │students │ │payments │ │competi- │ │edu_news │   │       │
│  │  │(유기체) │ │(학생)   │ │(결제)   │ │tors     │ │(뉴스)   │   │       │
│  │  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘   │       │
│  │       │           │           │           │           │         │       │
│  └───────┼───────────┼───────────┼───────────┼───────────┼─────────┘       │
│          │           │           │           │           │                  │
│          ▼           ▼           ▼           ▼           ▼                  │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │                [Layer 4: V 공식 계산 엔진]                       │       │
│  │                                                                  │       │
│  │   ┌─────────────────────────────────────────────────────────┐   │       │
│  │   │                                                         │   │       │
│  │   │     V = (M - T) × (1 + s)^t                            │   │       │
│  │   │                                                         │   │       │
│  │   │     M (Mint)    = 수강료 + 신규등록 + 재등록            │   │       │
│  │   │     T (Tax)     = 퇴원손실 + 미수금 + 운영비용          │   │       │
│  │   │     s (Synergy) = 만족도 × 추천율 × 연결강도            │   │       │
│  │   │     t (Time)    = 운영 기간 (복리 효과)                 │   │       │
│  │   │                                                         │   │       │
│  │   └─────────────────────────────────────────────────────────┘   │       │
│  │                                                                  │       │
│  └──────────────────────────────┬───────────────────────────────────┘       │
│                                 │                                           │
│                                 ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │                [Layer 5: AI 분석 & 예측]                         │       │
│  │                                                                  │       │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐             │       │
│  │  │ 퇴원 위험    │ │ 상담 스크립트│ │ 전략 추천    │             │       │
│  │  │ 예측        │ │ 생성        │ │ 생성        │             │       │
│  │  │ (Claude)    │ │ (Claude)    │ │ (Claude)    │             │       │
│  │  └──────────────┘ └──────────────┘ └──────────────┘             │       │
│  │                                                                  │       │
│  └──────────────────────────────┬───────────────────────────────────┘       │
│                                 │                                           │
│                                 ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │                [Layer 6: 액션 & 피드백]                          │       │
│  │                                                                  │       │
│  │  보상카드 → 원장 대시보드 → 승인 → n8n 실행 → 결과 Ledger      │       │
│  │                                                                  │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 데이터 융합 상세

### Step 1: 원시 데이터 → 정규화

각 데이터 소스에서 들어오는 다양한 형식을 **표준 스키마**로 변환

```typescript
// 학원 ERP에서 들어오는 데이터
{
  "source": "hagnara",
  "event_type": "attendance",
  "raw_data": {
    "student_id": "STU001",
    "date": "2026-01-20",
    "status": "출석",
    "class": "수학A"
  }
}

// → 정규화된 AUTUS 형식
{
  "entity_type": "student",
  "entity_id": "uuid-xxx",
  "metric": "attendance_rate",
  "value": 0.95,  // 95%
  "timestamp": "2026-01-20T09:00:00Z",
  "source": "hagnara"
}
```

### Step 2: 정규화 데이터 → V 공식 변수

```typescript
// 다양한 메트릭을 M, T, s로 매핑

const V_FORMULA_MAPPING = {
  // Mint (M) - 가치 유입
  mint: {
    "monthly_revenue": { weight: 1.0, type: "direct" },
    "new_enrollment": { weight: 350000, type: "count_to_value" },  // 1건 = 35만원
    "re_enrollment": { weight: 350000 * 6, type: "count_to_value" }, // 재등록 = 6개월 LTV
    "referral": { weight: 350000 * 0.1, type: "count_to_value" }  // 추천 보너스
  },
  
  // Tax (T) - 가치 유출
  tax: {
    "churn": { weight: 350000 * 6, type: "count_to_value" },  // 퇴원 = 6개월 손실
    "overdue_payment": { weight: 1.0, type: "direct" },
    "refund": { weight: 1.0, type: "direct" },
    "marketing_cost": { weight: 1.0, type: "direct" },
    "staff_turnover": { weight: 500000, type: "count_to_value" }  // 강사 이직
  },
  
  // Synergy (s) - 연결 강도
  synergy: {
    "satisfaction_score": { weight: 0.3, normalize: true },  // 0~1
    "recommendation_rate": { weight: 0.3, normalize: true },
    "parent_response_rate": { weight: 0.2, normalize: true },
    "homework_completion": { weight: 0.2, normalize: true }
  }
};
```

### Step 3: V 값 계산

```typescript
// V 계산 함수
function calculateV(organism: Organism): number {
  const M = calculateMint(organism);   // 총 유입 가치
  const T = calculateTax(organism);    // 총 유출 가치
  const s = calculateSynergy(organism); // 시너지 계수 (0~1)
  const t = getOperatingMonths(organism); // 운영 기간
  
  // V = (M - T) × (1 + s)^t
  const V = (M - T) * Math.pow(1 + s, t / 12); // 월 단위 복리
  
  return V;
}

// 예시 계산
const academy = {
  monthly_revenue: 5000000,      // 500만원/월
  new_enrollments: 5,            // 신규 5명
  churns: 2,                     // 퇴원 2명
  overdue: 500000,               // 미수금 50만원
  satisfaction: 0.85,            // 만족도 85%
  recommendation: 0.70,          // 추천율 70%
  operating_months: 24           // 운영 24개월
};

// M = 5,000,000 + (5 × 350,000) = 6,750,000
// T = (2 × 350,000 × 6) + 500,000 = 4,700,000
// s = (0.85 × 0.3) + (0.70 × 0.3) + ... ≈ 0.15
// t = 24

// V = (6,750,000 - 4,700,000) × (1.15)^2
// V = 2,050,000 × 1.3225
// V ≈ 2,711,125
```

---

## 🔗 데이터 융합 매트릭스

### 퇴원 예측 융합

```
┌─────────────────────────────────────────────────────────────┐
│                    퇴원 위험 점수 계산                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  데이터 소스          가중치    기여도                       │
│  ─────────────────────────────────────────────────────      │
│  ERP 출석률           40%       attendance < 80% → +40점    │
│  LMS 숙제제출         35%       homework < 60% → +35점      │
│  시험 성적변화        25%       grade_trend < -10 → +25점   │
│  상담 응답률          25%       response < 70% → +25점      │
│  결제 미납            30%       overdue > 7일 → +30점       │
│  ─────────────────────────────────────────────────────      │
│  총점: 0~200+점                                              │
│  ─────────────────────────────────────────────────────      │
│  0~29점   → 🟢 Low (정상)                                   │
│  30~49점  → 🟡 Medium (모니터링)                            │
│  50~79점  → 🟠 High (상담 필요)                             │
│  80+점    → 🔴 Critical (즉시 상담)                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 시장 환경 융합

```
┌─────────────────────────────────────────────────────────────┐
│                    전략 위협/기회 분석                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [위협 (T 증가 요인)]                                       │
│  ├─ 경쟁사 수 증가 (네이버 API) → 마케팅 비용 ↑            │
│  ├─ 경쟁사 평점 상승 → 이탈 위험 ↑                         │
│  ├─ 부정적 맘카페 후기 → 신규 등록 ↓                       │
│  └─ 정책 규제 강화 (뉴스) → 운영 비용 ↑                    │
│                                                             │
│  [기회 (M/s 증가 요인)]                                     │
│  ├─ 고밀도 아파트 단지 (지도) → 잠재 고객 ↑                │
│  ├─ 경쟁사 폐업 (뉴스) → 시장 점유율 기회                  │
│  ├─ 긍정적 후기 (커뮤니티) → 추천 증가                     │
│  └─ 입시 정책 변화 (뉴스) → 수요 증가                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 실시간 융합 대시보드

### 학원 V-Engine 뷰

```
┌─────────────────────────────────────────────────────────────┐
│  📊 [학원명] V-Engine 대시보드          2026-01-20 11:00    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  현재 V 값: ₩2,711,125                               │  │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │  │
│  │  전월 대비: +12.5% ↑                                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │ M (Mint)    │ │ T (Tax)     │ │ s (Synergy) │          │
│  │ ₩6,750,000  │ │ ₩4,700,000  │ │ 0.15        │          │
│  │ ▲ +8%      │ │ ▼ -5%      │ │ ▲ +0.02    │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  🔴 퇴원 위험 알림                                    │  │
│  │  ─────────────────────────────────────────────────   │  │
│  │  • 윤지우 (고3): 위험 202점 → 오늘 상담 필요         │  │
│  │  • 강민서 (중1): 위험 201점 → 오늘 상담 필요         │  │
│  │  • 정예준 (중2): 위험 83점 → 이번 주 상담            │  │
│  │                                                       │  │
│  │  💸 예상 퇴원 손실: ₩8,400,000                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  🗺️ 시장 환경                                        │  │
│  │  ─────────────────────────────────────────────────   │  │
│  │  반경 2km 경쟁사: 12개 (전월 +2)                     │  │
│  │  평균 평점: 4.2 (우리: 4.5 ✓)                        │  │
│  │  잠재 고객 단지: 5개 (3,500세대)                     │  │
│  │                                                       │  │
│  │  📰 최신 정책: "2027 수능 개편안 발표"               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 기술 구현

### Supabase 트리거 (자동 V 계산)

```sql
-- 데이터 변경 시 자동으로 V 재계산
CREATE OR REPLACE FUNCTION recalculate_organism_v()
RETURNS TRIGGER AS $$
BEGIN
  -- V = (M - T) × (1 + s)^t
  NEW.value_v := (NEW.mint - NEW.tax) * POWER(1 + NEW.synergy, 1);
  
  -- 상태 자동 결정
  NEW.urgency := LEAST(1.0, GREATEST(0.0, 
    (NEW.entropy * 0.4) + 
    ((1 - NEW.sync_rate) * 0.3) + 
    (NEW.friction * 0.3)
  ));
  
  IF NEW.urgency > 0.8 THEN
    NEW.status := 'urgent';
  ELSIF NEW.urgency > 0.6 THEN
    NEW.status := 'warning';
  ELSIF NEW.urgency < 0.3 THEN
    NEW.status := 'opportunity';
  ELSE
    NEW.status := 'stable';
  END IF;
  
  NEW.updated_at := NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

### n8n 융합 워크플로우

```json
{
  "name": "AUTUS Data Fusion Pipeline",
  "nodes": [
    {
      "name": "ERP Webhook",
      "type": "webhook",
      "events": ["attendance", "grade", "payment"]
    },
    {
      "name": "Normalize Data",
      "type": "function",
      "code": "return normalizeToAUTUSSchema(items);"
    },
    {
      "name": "Calculate Metrics",
      "type": "function",
      "code": "return { M: calcMint(items), T: calcTax(items), s: calcSynergy(items) };"
    },
    {
      "name": "Update Supabase",
      "type": "supabase",
      "operation": "upsert",
      "table": "organisms"
    },
    {
      "name": "Check Alerts",
      "type": "if",
      "condition": "urgency > 0.7"
    },
    {
      "name": "Generate Reward Card",
      "type": "http",
      "url": "{{$env.VERCEL_API}}/api/brain"
    },
    {
      "name": "Send Notification",
      "type": "http",
      "url": "{{$env.VERCEL_API}}/api/notify"
    }
  ]
}
```

### API 융합 엔드포인트

```typescript
// GET /api/fusion/dashboard?academyId=xxx
{
  "success": true,
  "data": {
    "v_engine": {
      "current_v": 2711125,
      "mint": 6750000,
      "tax": 4700000,
      "synergy": 0.15,
      "trend": "+12.5%"
    },
    "churn_risk": {
      "critical": 3,
      "high": 1,
      "potential_loss": 8400000
    },
    "market_environment": {
      "competitors": 12,
      "avg_rating": 4.2,
      "our_rating": 4.5,
      "opportunity_zones": 5
    },
    "recent_news": [
      { "title": "2027 수능 개편안 발표", "impact": "high" }
    ],
    "recommended_actions": [
      { "priority": 1, "action": "윤지우 학부모 상담", "reason": "퇴원 위험 202점" }
    ]
  }
}
```

---

## 📈 융합 효과

### Before (데이터 사일로)

```
❌ ERP 데이터 따로
❌ 결제 데이터 따로
❌ 시장 정보 수동 검색
❌ 직감으로 의사결정
```

### After (AUTUS 융합)

```
✅ 모든 데이터 → 단일 V 값
✅ 퇴원 위험 자동 예측
✅ 시장 환경 실시간 반영
✅ AI 기반 액션 추천
```

---

## 🎯 핵심 메시지

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   "데이터가 많아서 복잡한 게 아니다.                        │
│    데이터를 하나의 공식으로 융합하지 못해서 복잡한 것이다." │
│                                                             │
│   AUTUS는 모든 데이터를 V 공식으로 수렴시켜                │
│   원장님이 "숫자 하나"만 보면 되게 만든다.                  │
│                                                             │
│   V가 올라가면 잘하고 있는 것.                              │
│   V가 내려가면 조치가 필요한 것.                            │
│                                                             │
│   단순하다. 그래서 강력하다.                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

*AUTUS - 복잡한 데이터를 단순한 가치로*
