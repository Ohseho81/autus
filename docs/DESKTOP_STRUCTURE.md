# 바탕화면 → autus 폴더 구조 (정리 후)

## 온리쌤/올댓바스켓 일체화

| 위치 | 용도 |
|------|------|
| **autus/allthatbasket/** | **메인 앱** — React Native(Expo), 세션 타임라인·몰트봇·코치/학부모 앱. 배포·개발는 여기 기준. |
| **autus/올댓바스켓/** | **레거시/참고** — 이전 구조·빌드 산출물(.aab, .apk)·추가 문서. 필요 시 참고만. |

- 신규 개발·배포는 `allthatbasket` 에서 진행.
- `올댓바스켓` 에만 있는 문서/설정이 필요하면 `allthatbasket/docs` 로 복사해 사용.

## 바탕화면 정리 이력

- 문서/스크린샷 아카이브는 2026.02 삭제됨 (불필요한 사진·문서 전부 삭제).

## 삭제한 중복

- `files 2` (폴더) — `files2` 와 동일 내용
- `allThatBasket` (폴더) — `autus/kraton-v2` 에 동일 컴포넌트 있음
