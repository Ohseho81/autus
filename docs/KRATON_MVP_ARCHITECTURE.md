# 🎓 KRATON MVP Architecture

> "모든 데이터는 '사람 × 시간' 축의 노드다"
> "모든 판단은 자산이다"

## 📐 데이터 DNA 구조

### 핵심 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│                    KRATON Data DNA                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   모든 데이터 = 노드(Node) × 시간(Time)                          │
│                                                                  │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐       │
│   │  사람 노드   │ ←→ │  관계 노드   │ ←→ │  이벤트 노드 │       │
│   │  (Person)   │     │ (Relation)  │     │   (Event)   │       │
│   └─────────────┘     └─────────────┘     └─────────────┘       │
│         │                   │                   │                │
│         └───────────────────┼───────────────────┘                │
│                             ▼                                    │
│                    ┌─────────────────┐                          │
│                    │   시간축 (t)     │                          │
│                    │  모든 것은 시간  │                          │
│                    │   위에서 움직인다 │                          │
│                    └─────────────────┘                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 노드 유형

| 노드 유형 | 테이블 | 설명 |
|----------|--------|------|
| **사람** | `relational_nodes` | 학생, 학부모, 선생님, 직원 |
| **관계** | `relationship_sigmas` | 사람 간 시너지 (σ) |
| **이벤트** | `interaction_logs` | 상호작용 (Quick Tag, 통화, 상담) |
| **결정** | `decision_logs` | 원장의 판단 (자산화) |
| **메시지** | `message_logs` | 카카오 알림톡, SMS |
| **자산** | `message_assets` | 대화에서 추출된 가치 |

---

## 🧠 의사결정 로그 시스템

### 목적
> "원장의 판단 상수를 추출하여 AI가 학습하고 자동화"

### 흐름

```
┌────────────────────────────────────────────────────────────────────┐
│                    의사결정 학습 파이프라인                          │
├────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 수집                                                            │
│  ┌─────────────┐                                                   │
│  │ 원장의 결정  │ → decision_logs 테이블                            │
│  │ (모든 판단)  │   - 결정 유형                                      │
│  └─────────────┘   - 맥락 스냅샷 (당시 상황)                        │
│        │           - 고려한 요소들                                   │
│        │           - 결정 이유                                       │
│        ▼                                                            │
│  2. 추적                                                            │
│  ┌─────────────┐                                                   │
│  │ 결과 측정   │ → result_outcome, result_delta_v                  │
│  │ (30일 후)   │   - 성공/실패 여부                                  │
│  └─────────────┘   - V 변화량 측정                                  │
│        │                                                            │
│        ▼                                                            │
│  3. 패턴 추출                                                       │
│  ┌─────────────┐                                                   │
│  │  AI 분석    │ → decision_patterns 테이블                        │
│  │ (Claude)    │   - 조건 패턴                                      │
│  └─────────────┘   - 예측 결과                                      │
│        │           - 신뢰도                                         │
│        ▼                                                            │
│  4. 자동화                                                          │
│  ┌─────────────┐                                                   │
│  │ 자동 실행   │ → automation_level                                │
│  │ (승인 후)   │   - suggest: 제안만                                │
│  └─────────────┘   - pre_approve: 확인 후 승인                      │
│                    - auto_execute: 완전 자동                        │
│                                                                     │
└────────────────────────────────────────────────────────────────────┘
```

### 의사결정 유형

```typescript
type DecisionType =
  // 관계 관련
  | 'student_enrollment'     // 학생 등록 승인
  | 'student_withdrawal'     // 학생 퇴원 처리
  | 'teacher_assignment'     // 선생님 배정
  | 'class_change'           // 반 변경
  | 'schedule_adjustment'    // 일정 조정
  
  // 위험 관련
  | 'risk_intervention'      // 위험 개입 결정
  | 'churn_prevention'       // 이탈 방지 조치
  | 'complaint_resolution'   // 민원 해결
  
  // 재무 관련
  | 'discount_approval'      // 할인 승인
  | 'payment_exception'      // 결제 예외 처리
  | 'refund_approval'        // 환불 승인
  
  // 운영 관련
  | 'policy_change'          // 정책 변경
  | 'exception_approval'     // 예외 승인
  | 'escalation_decision';   // 에스컬레이션 판단
```

### 맥락 스냅샷 예시

```json
{
  "context_snapshot": {
    "risk_score": 0.72,
    "s_index": 0.38,
    "bond_strength": 0.45,
    "days_since_last_interaction": 21,
    "recent_interactions": [
      {"type": "call", "sentiment": -0.3, "date": "2026-01-20"},
      {"type": "quick_tag", "tag": "불만", "date": "2026-01-18"}
    ],
    "contributing_factors": [
      "성적 하락 3개월 연속",
      "학부모 연락 감소",
      "수업 결석 증가"
    ],
    "suggested_by_ai": true,
    "ai_suggestion": "즉시 상담 예약 권장"
  },
  "input_factors": {
    "considered": ["학부모 요청", "학생 태도 변화", "선생님 피드백"],
    "weight_given": {
      "학부모_요청": 0.4,
      "학생_태도": 0.35,
      "선생님_피드백": 0.25
    },
    "gut_feeling": "이 학생은 환경 변화가 필요해 보임"
  }
}
```

---

## 📱 카카오 알림톡 자산화

### 목적
> "모든 대화는 데이터이고, 데이터는 자산이다"

### 흐름

```
┌────────────────────────────────────────────────────────────────────┐
│                    알림톡 자산화 파이프라인                          │
├────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 발송/수신                                                       │
│  ┌─────────────────────────────────────────────────────────┐       │
│  │  KRATON → 카카오 알림톡 → 학부모/학생                    │       │
│  │  학부모/학생 → 답변 → KRATON                             │       │
│  └─────────────────────────────────────────────────────────┘       │
│                          │                                          │
│                          ▼                                          │
│  2. 로깅                                                            │
│  ┌─────────────┐                                                   │
│  │ message_logs │ ← 모든 메시지 저장                               │
│  │  테이블      │   - 템플릿 ID                                     │
│  └─────────────┘   - 발송 상태 (전송/읽음)                         │
│         │          - 관련 노드 연결                                 │
│         ▼                                                           │
│  3. AI 분석                                                         │
│  ┌─────────────┐                                                   │
│  │  Claude     │ → sentiment, intent, entities 추출                │
│  │  분석       │                                                    │
│  └─────────────┘                                                   │
│         │                                                           │
│         ▼                                                           │
│  4. 자산 추출                                                       │
│  ┌─────────────────┐                                               │
│  │ message_assets  │ ← 가치 있는 정보 노드화                       │
│  │ 테이블          │   - concern (우려)                             │
│  └─────────────────┘   - request (요청)                            │
│         │              - complaint (불만)                           │
│         │              - praise (칭찬)                              │
│         ▼                                                           │
│  5. 액션 연결                                                       │
│  ┌─────────────────────────────────────────────────────────┐       │
│  │  자산 → 위험 큐 → 의사결정 요청 → 원장 판단 → 자동화     │       │
│  └─────────────────────────────────────────────────────────┘       │
│                                                                     │
└────────────────────────────────────────────────────────────────────┘
```

### 자산 유형

| 유형 | 설명 | 액션 |
|------|------|------|
| `concern` | 우려사항 (이탈 신호) | 위험 큐 등록, 상담 예약 |
| `praise` | 칭찬/긍정 피드백 | S 점수 상승, 리워드 |
| `request` | 요청사항 | 태스크 생성 |
| `complaint` | 불만 | 에스컬레이션, 즉시 대응 |
| `suggestion` | 제안 | 검토 큐 등록 |
| `commitment` | 약속/확답 | 일정 등록 |
| `milestone` | 마일스톤 (성과) | 리포트 반영 |
| `insight` | 인사이트 (학습용) | 패턴 학습 |

### 예시: 학부모 답변 분석

```json
// 학부모 답변 원문
"선생님, 아이가 요즘 학원 가기 싫다고 해서 걱정이에요. 
다음 주 상담 가능할까요? 수업료 납부는 좀 늦어질 것 같아요."

// AI 분석 결과
{
  "sentiment_score": -0.4,
  "intent_detected": ["concern", "request", "payment_issue"],
  "key_entities": {
    "concerns": ["학원 거부감"],
    "requests": ["상담 요청"],
    "payment_issue": true
  },
  "assets": [
    {
      "type": "concern",
      "title": "학생 학원 거부감",
      "content": "아이가 학원 가기 싫다고 함",
      "importance": 0.8,
      "urgency": 0.7,
      "actionable": true,
      "action_required": "선생님과 학생 면담 필요"
    },
    {
      "type": "request",
      "title": "상담 요청",
      "content": "다음 주 상담 요청",
      "importance": 0.6,
      "urgency": 0.5,
      "actionable": true,
      "action_required": "상담 일정 잡기"
    },
    {
      "type": "concern",
      "title": "결제 지연 예고",
      "content": "수업료 납부 지연 예정",
      "importance": 0.5,
      "urgency": 0.3,
      "actionable": false
    }
  ]
}
```

---

## 🏗️ MVP 기술 스택

```
┌─────────────────────────────────────────────────────────────────┐
│                      KRATON MVP Stack                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Frontend                                                        │
│  ├─ Next.js 14 (App Router)                                     │
│  ├─ TypeScript                                                   │
│  ├─ Tailwind CSS + Shadcn/ui                                    │
│  └─ Zustand (상태 관리)                                          │
│                                                                  │
│  Backend                                                         │
│  ├─ Vercel Functions (Next.js API Routes)                       │
│  ├─ Supabase (PostgreSQL + Auth + RLS)                          │
│  └─ n8n (워크플로우 자동화)                                      │
│                                                                  │
│  AI                                                              │
│  ├─ Claude Sonnet (일반 분석)                                    │
│  ├─ Claude Opus (복잡한 판단)                                    │
│  └─ 향후: Ollama (로컬 비용 최적화)                              │
│                                                                  │
│  통합                                                            │
│  ├─ 카카오 알림톡 (비즈메시지)                                   │
│  ├─ 토스 페이먼츠 (결제)                                         │
│  └─ ERP 연동 (스마트스쿨, 클래스팅)                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 데이터 흐름

### 일일 사이클

```
┌──────────────────────────────────────────────────────────────────┐
│                     KRATON Daily Cycle                            │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐       │
│  │ 09:00   │    │ 12:00   │    │ 18:00   │    │ 21:00   │       │
│  │ Morning │ →  │ Midday  │ →  │ Evening │ →  │ Night   │       │
│  │ Sync    │    │ Alert   │    │ Report  │    │ Learn   │       │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘       │
│       │              │              │              │              │
│       ▼              ▼              ▼              ▼              │
│  ERP 데이터      위험 큐        일일 리포트     패턴 학습         │
│  동기화          체크           생성/발송       AI 분석          │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

### 이벤트 기반 흐름

```
Quick Tag 입력
     │
     ▼
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  1. interaction_logs에 저장                                      │
│  2. physics_metrics 업데이트 (트리거)                            │
│  3. risk_score 재계산                                            │
│  4. 임계값 초과 시 → risk_queue 등록                             │
│  5. 알림 발송 (원장/담당자)                                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 핵심 테이블 요약

| 카테고리 | 테이블 | 역할 |
|----------|--------|------|
| **사람 축** | `relational_nodes` | 학생/학부모/선생님 |
| | `org_members` | 조직 구성원 (원장/FSD/Optimus) |
| **관계 축** | `relationship_sigmas` | 시너지 계수 (σ) |
| | `relationship_densities` | 관계 밀도 (P) |
| **시간 축** | `interaction_logs` | 상호작용 기록 |
| | `time_records` | 시간 투입 기록 |
| **의사결정** | `decision_logs` | 원장 판단 로그 |
| | `decision_patterns` | 학습된 패턴 |
| **메시지** | `message_logs` | 알림톡/SMS 로그 |
| | `message_assets` | 추출된 자산 |
| **위험** | `risk_queue` | 이탈 위험 큐 |
| | `physics_metrics` | Physics 점수 |

---

## 🚀 다음 단계

### Phase 1: MVP (4주)
1. 기본 스키마 적용 (Supabase)
2. Quick Tag UI 구현
3. 위험 큐 대시보드
4. 카카오 알림톡 연동

### Phase 2: 자동화 (4주)
1. 의사결정 로그 수집
2. AI 분석 파이프라인
3. 패턴 추출 자동화
4. 자동 제안 시스템

### Phase 3: 고도화 (4주)
1. 판단 상수 추출
2. 자동 실행 레벨 설정
3. Moltbot 텔레그램 조종석
4. 성과 대시보드

---

## 📎 관련 파일

- **스키마**: `/backend/database/kraton_v2_schema.sql`
- **의사결정 스키마**: `/backend/database/kraton_decision_schema.sql`
- **마스터 스키마**: `/backend/database/autus_master_schema.sql`
- **API**: `/vercel-api/app/api/`
