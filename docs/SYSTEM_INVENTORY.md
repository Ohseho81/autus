# AUTUS 72³ 시스템 전체 현황

> **"측정된 현재에 법칙을 적용하여 미래를 계산하고, 실제와 비교하여 계속 개선한다"**

---

## 📊 전체 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                      AUTUS 72³ System                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐           │
│  │   Engine    │   │  Frontend   │   │   Backend   │           │
│  │ (18 files)  │   │ (50+ views) │   │  (5 APIs)   │           │
│  └─────────────┘   └─────────────┘   └─────────────┘           │
│                                                                 │
│  ┌─────────────┐                                               │
│  │  Simulator  │                                               │
│  │ (4 scripts) │                                               │
│  └─────────────┘                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Engine (TypeScript) - 18개 모듈

### 코어 정의
| 파일 | 역할 | 핵심 내용 |
|------|------|----------|
| `Physics72Definition.ts` | 72 노드 정의 | 6 법칙 × 12 성질 = 72 |
| `Node72DataDictionary.ts` | 노드 사전 | 각 노드 설명 및 측정법 |
| `AcademyTemplate.ts` | 학원 템플릿 | 25개 활성 노드, 임계값 |

### 인과 관계
| 파일 | 역할 | 핵심 내용 |
|------|------|----------|
| `CausalMatrix72.ts` | 72×72 인과 행렬 | **127개 연결** 정의 |
| `BayesianPrior.ts` | Prior 행렬 (10×10) | 초기 추정값 |
| `NonlinearEquations.ts` | 비선형 방정식 | 10개 핵심 노드 수식 |

### 물리 법칙
| 파일 | 역할 | 핵심 내용 |
|------|------|----------|
| `LaplaceLaws.ts` | 6개 라플라스 법칙 | 보존, 엔트로피, 관성, 마찰, 중력, 임계 |
| `PhysicsEngine72.ts` | 물리 엔진 | 상태 업데이트, 개입, 시나리오 |

### 예측 & 학습
| 파일 | 역할 | 핵심 내용 |
|------|------|----------|
| `StatePredictor.ts` | 상태 예측기 | X(t+1) = f(X(t), Action, Law) |
| `BayesianLaplace.ts` | Bayesian 학습 | Prior + Evidence = Posterior |
| `LearningLoop72.ts` | **학습 루프** | Gradient Descent + Momentum |
| `MonthlyLearningScheduler.ts` | 월간 자동화 | 스케줄, 리마인더, 인사이트 |

### 압력 & 계산
| 파일 | 역할 | 핵심 내용 |
|------|------|----------|
| `PressureCalculator.ts` | 압력 계산기 | Pressure = Delay × Exposure × Recovery |
| `NodeCalculator.ts` | 노드 계산기 | Raw Data → 72 Node 변환 |

### 데이터 & 해석
| 파일 | 역할 | 핵심 내용 |
|------|------|----------|
| `DataConnector.ts` | Supabase 연결 | CRUD + 학습 히스토리 |
| `CubeInterpreter.ts` | 큐브 해석기 | 좌표 → 의미 해석 |
| `CubeInterpreter72.ts` | 72 해석기 | 노드 ID → 설명 |

---

## 🖥️ Frontend Views - 19개 라우트

### 메인 (현재 사용)
| 해시 | 아이콘 | 이름 | 기능 |
|------|--------|------|------|
| `#transform` | 🎯 | **Control** (홈) | 12인 정렬 Control Deck |
| `#data` | 📊 | **Data** | 실제 데이터 입력 + 학습 |
| `#pressure` | 🎯 | **Pressure** | 압력맵 - 미루면 손해 확정 |
| `#learning` | 🔄 | **Learning** | 학습 루프 시각화 |
| `#simulator` | 🧪 | **Simulator** | Laplacian 시뮬레이터 |
| `#map` | 🗺️ | **Map** | Physics Map (노드 시각화) |
| `#matrix72` | ⚖️ | **72 Matrix** | 6×12 매트릭스 뷰 |
| `#prediction` | 🔮 | **Predict** | 72³ 물리 예측 |
| `#cube72` | 🧊 | **72³ Cube** | 3D 큐브 렌더러 |

### 레거시 (참고용)
| 해시 | 아이콘 | 이름 | 기능 |
|------|--------|------|------|
| `#trinity` | 👑 | Trinity | 트리니티 대시보드 |
| `#matrix` | 🔥 | 72-Type | 72타입 상호작용 |
| `#engine` | ⚡ | **Engine** | 72×72 변환 엔진 |
| `#cube` | 🎲 | Cube | Money Flow Cube |
| `#dashboard` | 📊 | Audit | AuditBoard 스타일 |
| `#unified` | ⚛️ | Unified | 통합 대시보드 |
| `#hexagon` | ⬡ | Hexagon | Hexagon UI |
| `#graph` | 🔗 | Graph | 144 노드 관계도 |
| `#quantum` | 🔮 | Quantum | Quantum 상태 |
| `#smb` | 🏪 | SMB | 소상공인 대시보드 |

---

## 🔌 Backend API - 20+ 라우터

### Core APIs
| 라우터 | 엔드포인트 | 기능 |
|--------|------------|------|
| **auth** | `/auth/*` | JWT/API Key 인증 |
| **audit** | `/audit/*` | 감사 로그 |
| **autus** | `/autus/*` | AUTUS 메인 |
| **edge** | `/edge/*` | Edge 네트워크 |
| **efficiency** | `/efficiency/*` | 효율성 분석 |
| **kernel** | `/kernel/*` | 커널 |
| **learning** | `/learning/*` | **학습 시스템 (V2)** |
| **smb** | `/smb/*` | SMB 대시보드 |
| **ontology** | `/ontology/*` | 72⁴ 온톨로지 |

### Extended APIs
| 라우터 | 엔드포인트 | 기능 |
|--------|------------|------|
| **cube** | `/api/cube/*` | Personal Cube |
| **flow** | `/api/flow/*` | 자금 흐름 |
| **keyman** | `/api/keyman/*` | Keyman 분석 |
| **notification** | `/api/notifications/*` | **알림 (신규)** |
| **scale** | `/api/scale/*` | Multi-Scale |
| **sovereign** | `/api/sovereign/*` | 삭제 최적화 |
| **strategy** | `/api/strategy/*` | 전략 결정 |

### API 예시
```
POST /entities/academy-001/snapshots  → 월간 데이터 추가
POST /entities/academy-001/learn       → 학습 실행
POST /entities/academy-001/predict     → 3개월 예측
GET  /entities/academy-001/pressure    → 현재 압력 상태
```

---

## 🐍 Python Simulator - 4개 스크립트

| 파일 | 역할 | 출력 |
|------|------|------|
| `learning_cycle.py` | **학습 사이클 실행** | Prior → Posterior 변환 |
| `laplacian_v2.py` | 차분 방정식 엔진 | 시나리오 시뮬레이션 |
| `report_generator.py` | 보고서 생성 | 마크다운 리포트 |
| `DATA_COLLECTION_FORM.md` | 데이터 수집 양식 | 입력 가이드 |

### 실행 결과 (대치영어학원)
```
Prior MSE:     0.0186
Posterior MSE: 0.0008
개선율:        +95.5%
```

---

## 📐 핵심 수학

### 72 = 6 × 12

```
물리법칙 6개: 보존, 흐름, 관성, 가속, 마찰, 인력
개체성질 12개: 현금/채권/부채/자본 + 수입/지출/투자/회수 + 고객/공급/경쟁/협력
```

### 차분 방정식

```
X(t+1) = f(X(t), U(t), θ)

고객: customers += new - churn
충성도: loyalty += Δ(decay, competition, service)
의존도: dependency += 0.005 - hire × 0.05
현금: cash += income × (1 - fee) - expense
```

### Bayesian Learning

```
Posterior = Prior + learning_rate × Error × Input^T

Error = Actual - Predicted
```

---

## 🗃️ 데이터베이스 스키마

| 테이블 | 용도 | 주요 컬럼 |
|--------|------|----------|
| `entities` | 개체 정보 | id, name, type, config |
| `node_snapshots` | 72 노드 스냅샷 | period, values (JSONB) |
| `learning_history` | 학습 히스토리 | mse, mae, adjustments |
| `causal_coefficients` | 학습된 계수 | from_node, to_node, value |
| `predictions` | 예측 기록 | predicted_values, actual_values |

---

## 📁 파일 구조

```
autus-unified/
├── frontend-react/
│   └── src/
│       ├── engine/           # 18개 엔진 모듈
│       │   ├── CausalMatrix72.ts       # 72×72 인과 행렬 (127개 연결)
│       │   ├── LearningLoop72.ts       # 학습 루프
│       │   ├── MonthlyLearningScheduler.ts
│       │   ├── DataConnector.ts        # Supabase 연결
│       │   └── ...
│       │
│       ├── components/       # 50+ UI 컴포넌트
│       │   ├── DataInputDashboard.tsx  # 데이터 입력
│       │   ├── LaplacianSimulator.tsx  # 시뮬레이터
│       │   ├── LearningLoopDemo.tsx    # 학습 UI
│       │   ├── PressureMap/            # 압력맵
│       │   └── ...
│       │
│       └── App.tsx           # 라우팅 (19개 뷰)
│
├── backend/
│   └── src/
│       ├── main.py           # FastAPI
│       ├── routers/          # 5개 API 라우터
│       └── ...
│
├── simulator/                # Python 시뮬레이터
│   ├── learning_cycle.py     # 학습 사이클
│   ├── laplacian_v2.py       # 차분 방정식
│   └── report_generator.py   # 보고서 생성
│
└── SYSTEM_INVENTORY.md       # 이 문서
```

---

## ✅ 완성 체크리스트

### 코어
- [x] 72 노드 정의 (6 법칙 × 12 성질)
- [x] 72×72 인과 행렬 (127개 연결)
- [x] 6개 라플라스 법칙
- [x] 차분 방정식 시스템

### 학습
- [x] Bayesian Learning (Prior → Posterior)
- [x] Gradient Descent + Momentum
- [x] 월간 자동화 스케줄러
- [x] 인사이트 자동 생성

### 예측
- [x] 상태 예측기 (StatePredictor)
- [x] 시나리오 비교 (6개)
- [x] 임계점 감지 (CRITICAL, WARNING)

### UI
- [x] 19개 뷰 (라우트)
- [x] 데이터 입력 대시보드
- [x] 학습 루프 시각화
- [x] Laplacian 시뮬레이터
- [x] 압력맵 (Pressure Map)

### 백엔드
- [x] FastAPI 구조
- [x] **20+ API 라우터** (업데이트)
- [x] Supabase 연결 스키마
- [x] **JWT + API Key 인증** (신규)
- [x] **알림 시스템** (신규)
- [x] **ROF 추천 엔진** (신규)

### Python
- [x] 학습 사이클 (95.5% 개선 검증)
- [x] 보고서 생성기
- [x] 데이터 수집 양식

---

## 🚀 접근 방법

```bash
# 프론트엔드
http://localhost:3000/#data       # 데이터 입력
http://localhost:3000/#simulator  # 시뮬레이터
http://localhost:3000/#learning   # 학습 루프
http://localhost:3000/#pressure   # 압력맵
http://localhost:3000/#engine     # 72×72 엔진

# 백엔드
http://localhost:8000/docs        # API 문서

# Python
cd simulator && python learning_cycle.py
```

---

## 📈 다음 단계

```
🔜 Supabase 실제 연결
🔜 첫 실제 데이터 입력
🔜 첫 실제 학습 사이클
🔜 다른 도메인 확장 (식당, 프리랜서)
```

---

**Total: 18 Engine + 19 Views + 20+ APIs + 4 Scripts + 3 Test Suites = 60+ Components**

### 신규 추가 (2026-01-08)
- `recommendation_engine.py` - ROF Framework + 7가지 메타 추천
- `learning_engine.py` (업데이트) - IntegratedLearningLoop
- `notification_service.py` - 4채널 알림 시스템
- `auth/middleware.py` - JWT + API Key + Rate Limit
- `LearningPageV2.tsx` - ROF 기반 학습 UI
- `useLearning.ts` - Learning API Hook
- `useAuth.ts` - 인증 Hook
- `supabase.ts` - Supabase 클라이언트

*"72³ = 미래를 계산하는 시스템"*
