"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12


"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12


"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12


"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12


"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12












"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12


"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12


"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12


"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12


"""
AUTUS Realtime Config
=====================
설정값
"""

import os
from pathlib import Path

# 기본 경로
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / "data"

# 상태/감사 경로
STATE_PATH = str(DATA_DIR / "output" / "state.json")
AUDIT_PATH = str(DATA_DIR / "output" / "audit.jsonl")

# 입력 경로
MONEY_CSV = str(DATA_DIR / "input" / "money_events.csv")
BURN_CSV = str(DATA_DIR / "input" / "burn_events.csv")

# 서버 설정
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", 8000))

# 엔진 설정
ROLLING_WINDOW_DAYS = 7
DEFAULT_TEAM_SIZE = 5
TOP_K_CANDIDATES = 12

















