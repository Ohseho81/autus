# 🚀 온리쌤 v1.0 최종 배포 가이드

> **완성된 기능**: Solapi 알림톡 통합 + 데이터베이스 마이그레이션 시스템 + 앱 내 업데이트 버튼

---

## 📋 변경 사항 요약

### 1. 📱 Solapi 알림톡 통합 (완료)
- 12개 템플릿 지원 (출석 3종, 결제 3종, 스케줄 3종, 피드백 3종)
- CoachHomeScreen 출석 체크 시 자동 알림 발송
- Event Ledger 자동 기록
- Dev 모드 지원 (실제 발송 없이 로그만 출력)

### 2. 🔧 데이터베이스 마이그레이션 시스템 (완료)
- `migrations` 폴더 생성 (001_data_cleanup.sql)
- migrationService 구현 (중복 실행 방지, 실행 시간 기록)
- Supabase Edge Function (`run-migration`)
- 앱 내 "데이터베이스 업데이트" 버튼 (SettingsScreen)

### 3. 🎯 시스템 관리 기능 (완료)
- SettingsScreen에 "시스템 관리" 섹션 추가
- "데이터베이스 업데이트" 버튼 (마이그레이션 실행)
- "시스템 상태" 버튼 (system_health 뷰 조회)

---

## 🛠️ 배포 전 체크리스트

### Step 1: Supabase 설정 (10분)

#### 1.1 exec_sql Function 생성
```sql
-- Supabase Dashboard > SQL Editor에서 실행
-- 파일: /supabase_exec_sql_function.sql
```

**실행 방법:**
1. Supabase Dashboard 접속: https://supabase.com/dashboard/project/dcobyicibvhpwcjqkmgw
2. 좌측 메뉴에서 "SQL Editor" 클릭
3. `/sessions/modest-bold-einstein/mnt/autus/supabase_exec_sql_function.sql` 내용 복사
4. "Run" 버튼 클릭

**예상 결과:**
```
✅ exec_sql function created successfully!
```

#### 1.2 Edge Function 배포

**방법 A: Supabase CLI 사용 (권장)**
```bash
# Supabase CLI 설치 (없으면)
npm install -g supabase

# 로그인
supabase login

# 프로젝트 연결
supabase link --project-ref dcobyicibvhpwcjqkmgw

# Edge Function 배포
supabase functions deploy run-migration
```

**방법 B: 수동 배포 (CLI 없이)**
1. Supabase Dashboard > Edge Functions
2. "New Function" 클릭
3. Function Name: `run-migration`
4. `/sessions/modest-bold-einstein/mnt/autus/온리쌤/supabase/functions/run-migration/index.ts` 내용 복사 붙여넣기
5. Deploy 클릭

**환경 변수 설정:**
```bash
# Edge Function이 자동으로 접근 가능:
# - SUPABASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
```

---

### Step 2: Solapi 설정 (30분)

#### 2.1 Solapi 계정 생성
1. https://solapi.com 접속
2. 회원가입 (무료 크레딧 5,000원 지급)

#### 2.2 API Key 발급
1. 대시보드 > API Key 관리
2. "새 API Key 생성" 클릭
3. API Key와 API Secret 복사 → `.env` 파일에 저장

#### 2.3 발신번호 등록
1. 대시보드 > 발신번호 관리
2. 학원 전화번호 등록
3. 인증 완료

#### 2.4 카카오 채널 연동
1. https://business.kakao.com 에서 채널 개설
2. Solapi 대시보드 > 카카오 채널 연동
3. 카카오 채널 ID (PFID) 입력

#### 2.5 알림톡 템플릿 등록

**파일 참고:** `/sessions/modest-bold-einstein/mnt/autus/온리쌤/Solapi_알림톡_연동_가이드.md`

**12개 템플릿:**
1. ATTENDANCE_CONFIRM (출석 확인)
2. ABSENCE_NOTICE (결석 알림)
3. LATE_NOTICE (지각 알림)
4. PAYMENT_REQUEST (결제 요청)
5. PAYMENT_COMPLETE (결제 완료)
6. PAYMENT_OVERDUE (미납 알림)
7. CLASS_REMINDER (수업 리마인드)
8. SCHEDULE_CHANGE (스케줄 변경)
9. CLOSURE_NOTICE (휴원 공지)
10. CLASS_RESULT (수업 결과)
11. ACHIEVEMENT (성취 축하)
12. CONSULTATION_REQUEST (상담 요청)

**템플릿 등록 절차:**
1. Solapi 대시보드 > 템플릿 관리
2. "새 템플릿" 클릭
3. 템플릿 ID와 내용 입력 (가이드 참고)
4. 검수 신청 (1~3일 소요)

---

### Step 3: 환경 변수 설정 (5분)

**`.env` 파일 생성:**
```bash
cp .env.example .env
```

**필수 환경 변수:**
```env
# Supabase
EXPO_PUBLIC_SUPABASE_URL=https://dcobyicibvhpwcjqkmgw.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Solapi
SOLAPI_API_KEY=your-solapi-api-key
SOLAPI_API_SECRET=your-solapi-api-secret
KAKAO_PFID=your-kakao-channel-id
SENDER_PHONE=01012345678
```

---

### Step 4: 의존성 설치 및 빌드 (5분)

```bash
cd /sessions/modest-bold-einstein/mnt/autus/온리쌤

# 의존성 설치
npm install

# TypeScript 컴파일 확인
npx tsc --noEmit

# 앱 실행 (Dev 모드)
npm start
```

**빌드 확인:**
```bash
# Android 빌드
eas build --platform android --profile preview

# iOS 빌드
eas build --platform ios --profile preview
```

---

## 🧪 테스트 시나리오

### 테스트 1: Solapi 알림톡 (Dev 모드)

**목적:** 알림톡 발송 로직 확인 (실제 발송 X)

**절차:**
1. `.env`에서 `NODE_ENV=development` 확인
2. 앱 실행 → 코치 모드 로그인
3. CoachHomeScreen → 출석 체크
4. 콘솔에서 `[AlimtalkService] DEV MODE` 로그 확인

**예상 결과:**
```
[AlimtalkService] DEV MODE - Message not sent: {
  to: "01012345678",
  templateId: "ATTENDANCE_CONFIRM",
  variables: { name: "홍길동", class_name: "배구 A반", ... }
}
```

---

### 테스트 2: 데이터베이스 마이그레이션

**목적:** 앱에서 마이그레이션 실행 확인

**절차:**
1. 앱 실행 → 관리자 모드 로그인
2. 하단 탭 → "설정" (⚙️)
3. "시스템 관리" 섹션으로 스크롤
4. "데이터베이스 업데이트" 버튼 클릭
5. 확인 다이얼로그 → "업데이트" 클릭

**예상 결과:**
```
✅ 업데이트 완료
1개의 마이그레이션이 성공적으로 완료되었습니다.
실행 시간: 2543ms
```

**검증:**
1. "시스템 상태" 버튼 클릭
2. 통계 확인:
   - 학원: 1개
   - 프로필 universal_id 연결: 0개 미연결
   - 평균 V-Index: 100.00°

---

### 테스트 3: 시스템 상태 조회

**목적:** system_health 뷰 정상 작동 확인

**절차:**
1. 설정 → 시스템 관리 → "시스템 상태" 클릭

**예상 결과:**
```
📊 시스템 통계

• 학원: 1개
• 전체 프로필: 780개
• 학생: 780명
• 코치: 0명
• 평균 V-Index: 100.00°
• 전체 이벤트: 1,003개
• 최근 7일 이벤트: 245개
• 대기 태스크: 0개
```

---

### 테스트 4: Solapi 알림톡 (Production 모드)

**⚠️ 주의:** 실제 SMS 비용 발생 (9원/건)

**목적:** 실제 알림톡 발송 확인

**절차:**
1. `.env`에서 `NODE_ENV=production` 설정
2. Solapi 템플릿 검수 승인 완료 확인
3. 테스트용 전화번호로 출석 체크
4. 카카오톡 알림 수신 확인

**예상 결과:**
- 카카오톡에 "출석 확인" 알림 수신
- Event Ledger에 `notification_sent` 이벤트 기록

**확인 쿼리:**
```sql
SELECT * FROM event_ledger
WHERE event_type = 'notification_sent'
AND metadata->>'type' = 'alimtalk'
ORDER BY created_at DESC
LIMIT 10;
```

---

## 📊 배포 후 모니터링

### Supabase Dashboard

**Event Ledger 확인:**
```sql
-- 최근 24시간 알림 발송 현황
SELECT
  metadata->>'template' as template,
  COUNT(*) as count,
  SUM(CASE WHEN metadata->>'success' = 'true' THEN 1 ELSE 0 END) as success_count
FROM event_ledger
WHERE event_type = 'notification_sent'
  AND created_at > NOW() - INTERVAL '24 hours'
GROUP BY metadata->>'template'
ORDER BY count DESC;
```

**마이그레이션 실행 이력:**
```sql
SELECT * FROM migrations
ORDER BY executed_at DESC;
```

**시스템 상태:**
```sql
SELECT * FROM system_health;
```

---

### Solapi Dashboard

**발송 내역 확인:**
1. Solapi 대시보드 > 메시지 > 발송 내역
2. 성공/실패율 확인
3. 비용 확인 (9원/건 × 발송 건수)

**예상 월간 비용:**
- 일일 40명 출석 × 30일 = 1,200건
- 1,200건 × 9원 = 10,800원/월

---

## 🐛 트러블슈팅

### 문제 1: Edge Function 호출 실패

**증상:**
```
Error: Edge Function 'run-migration' not found
```

**해결:**
1. Supabase Dashboard > Edge Functions 확인
2. Function이 배포되었는지 확인
3. 없으면 Step 1.2 다시 실행

---

### 문제 2: exec_sql 권한 오류

**증상:**
```
permission denied for function exec_sql
```

**해결:**
```sql
-- service_role 권한 부여
GRANT EXECUTE ON FUNCTION exec_sql(TEXT) TO service_role;
```

---

### 문제 3: 알림톡 발송 실패

**증상:**
```
[AlimtalkService] Send error: Template not found
```

**해결:**
1. Solapi 대시보드에서 템플릿 ID 확인
2. 템플릿 검수 승인 여부 확인
3. `src/types/alimtalk.ts`의 템플릿 ID와 Solapi 템플릿 ID 일치 확인

---

### 문제 4: 마이그레이션 중복 실행

**증상:**
```
Migration 001 already executed. Skipping...
```

**해결:**
- 정상 동작입니다. 중복 실행 방지 기능이 작동한 것입니다.
- 강제 재실행이 필요하면:
```sql
DELETE FROM migrations WHERE version = '001';
```

---

## 📦 생성된 파일 목록

### 새로 생성된 파일
```
📁 온리쌤/
├── 📁 src/
│   ├── 📁 config/
│   │   └── solapiConfig.ts (NEW)
│   ├── 📁 types/
│   │   └── alimtalk.ts (NEW)
│   ├── 📁 services/
│   │   ├── alimtalkService.ts (NEW)
│   │   └── migrationService.ts (NEW)
│   ├── 📁 migrations/
│   │   └── 001_data_cleanup.sql (NEW)
│   └── 📁 screens/v2/
│       ├── CoachHomeScreen.tsx (MODIFIED)
│       └── SettingsScreen.tsx (MODIFIED)
├── 📁 supabase/
│   └── 📁 functions/
│       └── 📁 run-migration/
│           └── index.ts (NEW)
├── package.json (MODIFIED - axios 추가)
├── .env.example (MODIFIED - Solapi + Service Role Key)
├── supabase_exec_sql_function.sql (NEW)
├── 배포_가이드_v1.0.md (NEW)
└── Solapi_알림톡_연동_가이드.md (EXISTING)
```

### 수정된 파일
```
- src/screens/v2/CoachHomeScreen.tsx (출석 체크 시 알림 발송)
- src/screens/v2/SettingsScreen.tsx (시스템 관리 섹션 추가)
- package.json (axios 의존성)
- .env.example (Solapi, Service Role Key)
```

---

## ✅ 최종 체크리스트

배포 전 다음 항목을 모두 확인하세요:

### Supabase
- [ ] exec_sql Function 생성 완료
- [ ] run-migration Edge Function 배포 완료
- [ ] Service Role Key 환경 변수 설정 완료

### Solapi
- [ ] Solapi 계정 생성 및 크레딧 충전
- [ ] API Key/Secret 발급 및 .env 설정
- [ ] 발신번호 등록 및 인증 완료
- [ ] 카카오 채널 개설 및 연동
- [ ] 12개 알림톡 템플릿 등록 및 검수 승인

### 앱
- [ ] 의존성 설치 (npm install)
- [ ] TypeScript 컴파일 확인 (npx tsc --noEmit)
- [ ] .env 파일 설정 완료
- [ ] Dev 모드 테스트 완료
- [ ] 빌드 성공 확인

### 테스트
- [ ] 알림톡 Dev 모드 테스트
- [ ] 데이터베이스 마이그레이션 실행
- [ ] 시스템 상태 조회
- [ ] (선택) Production 알림톡 발송 테스트

---

## 🎉 완료!

모든 체크리스트를 완료했다면, **온리쌤 v1.0이 배포 준비 완료**되었습니다!

**다음 단계:**
1. 앱 스토어 배포 (eas build --platform all)
2. 사용자 피드백 수집
3. 추가 알림톡 템플릿 개발 (결제, 상담 등)

**문의:**
- Supabase: https://supabase.com/dashboard
- Solapi: https://docs.solapi.com
- AUTUS: /sessions/modest-bold-einstein/mnt/autus/CLAUDE.md

---

**생성일:** 2026-02-14
**버전:** v1.0
**작성자:** Claude Sonnet 4.5 (AUTUS 6-Agent System)
