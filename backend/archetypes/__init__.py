"""
═══════════════════════════════════════════════════════════════════════════════
🎭 AUTUS Archetypes Module (아키타입 모듈)
═══════════════════════════════════════════════════════════════════════════════

10개 핵심 아키타입으로 80억 인류를 표현

"5%의 완벽한 틀 + 물리법칙 = 100% 살아있는 우주"
═══════════════════════════════════════════════════════════════════════════════
"""

import json
import os
from pathlib import Path

# JSON 파일 로드
_archetypes_path = Path(__file__).parent / "autus_archetypes.json"

def load_archetypes():
    """아키타입 데이터 로드"""
    with open(_archetypes_path, "r", encoding="utf-8") as f:
        return json.load(f)

ARCHETYPES_DATA = load_archetypes()
ARCHETYPES = {a["id"]: a for a in ARCHETYPES_DATA["archetypes"]}

__all__ = ["ARCHETYPES", "ARCHETYPES_DATA", "load_archetypes"]
