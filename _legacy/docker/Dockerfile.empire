# ═══════════════════════════════════════════════════════════════════════════════
#                    🏛️ AUTUS EMPIRE - Docker Configuration
# ═══════════════════════════════════════════════════════════════════════════════

FROM python:3.11-slim

# 메타데이터
LABEL maintainer="AUTUS Empire <autus@empire.io>"
LABEL version="4.0.0"
LABEL description="AUTUS Empire Final Form - Integrated Operations System"

# 작업 디렉토리 설정
WORKDIR /app

# 시스템 의존성 설치 (OpenCV용)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    libpq-dev \
    libglib2.0-0 \
    libsm6 \
    libxext6 \
    libxrender-dev \
    libgl1-mesa-glx \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Python 의존성 복사 및 설치
COPY requirements.empire.txt .
RUN pip install --no-cache-dir -r requirements.empire.txt

# 애플리케이션 코드 복사
COPY backend/ ./backend/
COPY backend/main_final.py ./main_final.py

# 데이터 디렉토리 생성
RUN mkdir -p /app/data

# 환경 변수
ENV PYTHONUNBUFFERED=1
ENV PORT=8000
ENV HOST=0.0.0.0

# 헬스체크
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:${PORT}/health || exit 1

# 포트 노출
EXPOSE ${PORT}

# 실행 명령
CMD ["sh", "-c", "uvicorn main_final:app --host ${HOST} --port ${PORT}"]

