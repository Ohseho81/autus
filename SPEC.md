# AUTUS SPEC v1.0

> **"Human decides once → System closes forever"**

---

## 📋 개요

| 항목 | 값 |
|------|-----|
| **이름** | AUTUS (Autonomous Twin Universal System) |
| **버전** | 1.0 |
| **목적** | 필리핀 석사+취업 프로젝트 의사결정 자동화 |
| **배포** | Railway (`autus-production.up.railway.app`) |
| **프론트엔드** | `solar.autus-ai.com` |

---

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    AUTUS ARCHITECTURE                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐              │
│  │  Google  │───▶│  AUTUS   │───▶│  SOLAR   │              │
│  │  Sheets  │    │   API    │    │    UI    │              │
│  └──────────┘    └──────────┘    └──────────┘              │
│       │               │               │                     │
│   데이터 입력     물리 계산      시각화/실행               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📂 폴더 구조

```
autus/
├── app/           # FastAPI 백엔드 (2,122줄)
│   ├── main.py    # 메인 API (60개 엔드포인트)
│   ├── api/       # 추가 라우터
│   ├── models/    # Pydantic 모델
│   └── routes/    # Sheets API 등
├── core/          # 핵심 비즈니스 로직
├── engines/       # 물리 엔진 (Telemetry)
├── frontend/      # UI
│   ├── index.html # 진입점
│   └── solar.html # ELON EDITION (메인)
├── connectors/    # 외부 연결
│   └── sheets.py  # Google Sheets 연동
├── config/        # 설정
├── tests/         # 테스트 (116개)
├── docs/          # 문서
├── static/        # 정적 파일
├── scripts/       # 유틸리티
└── archive/       # 이전 코드 보관
```

---

## 🌐 API 엔드포인트

### 핵심 API

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/health` | 서버 상태 |
| GET | `/api/v1/state` | 현재 상태 (9 Planets) |
| POST | `/api/v1/burn/simulate` | 액션 시뮬레이션 |
| POST | `/api/v1/burn/execute` | 액션 실행 |
| GET | `/api/v1/stream/state` | SSE 실시간 스트림 |

### Google Sheets API

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/v1/sheets/status` | 연결 상태 |
| GET | `/api/v1/sheets/planets` | 9 Planets 계산 |
| GET | `/api/v1/sheets/snapshot` | 스냅샷 (Frontend용) |
| GET | `/api/v1/snapshot/current` | 현재 스냅샷 |

### P0 Task API

| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | `/api/v1/task/cost/analyze` | 비용 분석 |
| POST | `/api/v1/task/cost/decide` | 비용 결정 |
| POST | `/api/v1/task/schedule/analyze` | 일정 분석 |
| POST | `/api/v1/task/risk/analyze` | 리스크 분석 |
| POST | `/api/v1/task/decision/share` | 결정 공유 |

---

## 🪐 9 Planets 물리 모델

### 입력 (Google Sheets)

| 시트 | 컬럼 | 설명 |
|------|------|------|
| 인력 | 이름, 상태 | 재학중/인턴/취업/이탈 |
| 재정 | 구분, 금액 | 수입/지출 |
| 파트너 | 유형, 상태 | 대학/기업/정부 |
| 이슈 | 상태 | 신규/진행중/해결/미해결 |

### 물리량 계산

```
Output   = 재학생 × ₩500만
Quality  = 취업자 × ₩100만
Shock    = 이탈자 × ₩2,000만
Friction = 미해결이슈 × ₩50만
Cohesion = 활성파트너 × ₩1,000만
Stability = 현금 잔액
Recovery = 이슈해결률 (%)
```

### 파생값

```
Risk     = (Shock + Friction) / (Output + Cohesion) × 100
Entropy  = 미해결이슈 / 총인원 × 100
Pressure = |적자| / ₩1,000만 × 100 (적자 시)
Flow     = (재학 + 취업) / 총인원 × 100
```

### GATE 결정

```
GREEN : Risk < 30% AND Shock = 0
AMBER : Risk < 60%
RED   : Risk ≥ 60%
```

---

## 🖥️ Frontend (ELON EDITION)

### 원칙

```
요소: 5개만
설명: 0
자동 실행: 3초
클릭: 0회
```

### UI 요소

```
┌─────────────────────────────────────────────────────────────┐
│  GATE: GREEN                                      120 FPS   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                      ○   ○   ○                              │
│                    ○           ○                            │
│                   ○      ☀️      ○                          │
│                    ○           ○                            │
│                      ○   ○   ○                              │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                       💰 −8%                                │
│              ████████ RECOVER ████████                      │
│                         3                                   │
└─────────────────────────────────────────────────────────────┘
```

### 5개 요소

1. **GATE 배지** - 상태 (GREEN/AMBER/RED)
2. **FPS 카운터** - 실시간성 표시
3. **💰 Impact** - 손실/절감 %
4. **RECOVER 버튼** - 유일한 액션
5. **카운트다운** - 3초 자동 실행

### 자동 실행 흐름

```
0초: 화면 로드 → API에서 상태 가져오기
1초: 카운트다운 2
2초: 카운트다운 1 (깜빡임)
3초: 자동 RECOVER 실행
3.3초: ✓ RECOVERED 표시
6초: 새로고침 → 안정 상태
```

---

## 🔌 Google Sheets 연동

### 환경변수 (Railway)

```
GOOGLE_SHEET_ID=your-sheet-id
GOOGLE_CREDENTIALS_JSON={"type":"service_account",...}
```

### 데이터 흐름

```
Google Sheets 수정
       ↓ (30초마다 동기화)
AUTUS API (물리 계산)
       ↓
Solar HQ (실시간 반영)
```

---

## 📊 현재 상태 (API 응답)

```json
{
  "status": "STABLE",
  "fps": 120,
  "planets": {
    "recovery": 0.1,
    "stability": 1.0,
    "cohesion": 0.1,
    "shock": 0.1,
    "friction": 0.1,
    "time": 1.0,
    "quality": 0.4,
    "output": 0.1
  },
  "twin": {
    "entropy": 0.0,
    "pressure": 0.0,
    "risk": 0.0,
    "flow": 0.13
  }
}
```

---

## 🚀 배포

### Railway

```yaml
# railway.toml
[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
```

### CI/CD

```yaml
# .github/workflows/ci-cd.yml
- lint (ruff, black)
- test (pytest)
- security (safety)
- deploy (Railway auto-deploy)
```

---

## 📈 KPI

| 지표 | 목표 |
|------|------|
| 결정 시간 | ≤ 3초 (자동) |
| 클릭 수 | 0회 |
| RECOVER 실행률 | ≥ 70% |
| API 응답시간 | ≤ 100ms |
| 가동률 | ≥ 99.9% |

---

## 🔐 보안

- API Key 인증 (AUTUS_API_KEY)
- CORS 설정
- Rate Limiting (60/min)
- Protected 엔드포인트: `/execute`, `/burn/execute`

---

## 📝 핵심 원칙

1. **결정 전 자동화**: 정리·요약·추천
2. **결정 순간**: 인간 1클릭 (또는 자동)
3. **결정 후 자동화**: 봉인·증빙·적용·학습
4. **금지**: 자동 결정 (Risk > 50%), 되돌리기

---

## 📚 문서

| 문서 | 위치 |
|------|------|
| README | `/README.md` |
| WHITEPAPER | `/docs/WHITEPAPER.md` |
| PHYSICS | `/docs/PHYSICS.md` |
| API Reference | `/docs/API_REFERENCE.md` |

---

## 🎯 요약

```
AUTUS = Google Sheets + Physics Engine + Auto-Execute UI

입력: 인력/재정/파트너/이슈 (Sheets)
계산: 9 Planets → Risk/Entropy/Pressure/Flow
출력: GATE (GREEN/AMBER/RED) + 3초 자동 RECOVER
결과: Human decides once → System closes forever
```

---

**Version**: 1.0  
**Last Updated**: 2025-12-19  
**Maintainer**: AUTUS Team
