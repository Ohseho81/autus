# ═══════════════════════════════════════════════════════════════════════════════
# 🏛️ AUTUS 2.0 - Cursor AI 개발 가이드
# ═══════════════════════════════════════════════════════════════════════════════
#
# AUTUS = 오너의 조종석 (Cursor = 제조 공장)
# "목표를 던지고, 예외만 승인하고, 결과를 확인한다"
#
# ═══════════════════════════════════════════════════════════════════════════════

## 🎯 프로젝트 개요

AUTUS는 **관계 유지력 OS**로, A = R^σ 공식을 기반으로 합니다.
- R (Relationship): TSEL 기반 관계지수
- σ (Sigma): 환경지수 (Internal + Voice + External)
- A (Attraction): 유지력 = R^σ

### 현재 산업: 학원 (KRATON)
- 용어: 학생, 학부모, 강사, 재원수, 퇴원
- TSEL 가중치: T=0.25, S=0.30, E=0.25, L=0.20

### 핵심 철학
- **Zero Meaning**: 데이터는 의미 없는 숫자(node_id, value, timestamp)로 변환
- **Money Physics**: 돈의 흐름을 물리 법칙으로 모델링
- **5-Tier System**: T1(허브) → T2(커넥터) → T3(액티브) → T4(일반) → Ghost(비활성)
- **관찰자 모드**: 개입 없이 자연 흐름 관찰

### 공식
```
V = (M - T) × (1 + s)^t
SQ = (Mint - Burn) / Time × Synergy_Factor
```

## 📁 프로젝트 구조

```
autus/
├── autus-unified/           # 🎯 메인 프로젝트 (여기서 개발)
│   ├── backend/             # FastAPI 백엔드
│   │   ├── main.py          # API 진입점
│   │   ├── auth/            # JWT + API Key 인증
│   │   ├── webhooks/        # Stripe/Shopify/토스
│   │   ├── autosync/        # 30+ SaaS 자동 감지
│   │   ├── crewai/          # AI 분석
│   │   ├── parasitic/       # SaaS 흡수/대체
│   │   ├── physics/         # 물리 엔진 (핵심!)
│   │   ├── integrations/    # Zero Meaning, 메타데이터
│   │   └── websocket/       # 실시간 통신
│   ├── frontend/            # Physics Map HTML
│   │   ├── physics-map-unified.html      # D3.js 2D
│   │   ├── physics-map-satellite.html    # Leaflet 위성
│   │   └── physics-map-globe-5tier.html  # Three.js 3D 지구본
│   ├── tests/               # pytest 테스트
│   └── docker-compose.yml   # 컨테이너 설정
│
├── _legacy/                 # 레거시 (참고용)
├── autus_pipeline/          # 데이터 파이프라인
├── docs/                    # 문서
└── Makefile                 # 개발 명령어
```

## 🚀 빠른 시작 명령어

```bash
# 의존성 설치
make install

# 백엔드 서버 실행
make dev

# 프론트엔드 서버 실행 (별도 터미널)
make frontend

# 테스트 실행
make test

# 코드 포맷팅
make fix
```

## 🔧 개발 규칙

### 1. Python 코드 스타일
- Ruff 사용 (린트 + 포맷)
- 타입 힌트 필수
- Pydantic 모델 사용
- async/await 패턴

### 2. API 설계
```python
# 라우터 패턴
from fastapi import APIRouter
router = APIRouter(prefix="/module", tags=["Module"])

@router.post("/action")
async def action(request: RequestModel) -> ResponseModel:
    pass
```

### 3. 응답 형식
```python
{
    "success": True,
    "data": {...},
    "message": "설명"
}
```

### 4. 에러 처리
```python
from fastapi import HTTPException
raise HTTPException(status_code=400, detail="에러 메시지")
```

## 🎨 Frontend 규칙

### Physics Map 파일 구조
- `physics-map-unified.html`: D3.js 기반, CUT/LINK, 메타데이터
- `physics-map-satellite.html`: Leaflet + 위성, 한국 지도
- `physics-map-globe-5tier.html`: Three.js 3D 지구본

### 5-Tier 색상
```javascript
const TIERS = {
    T1: { color: '#FFD700', name: '허브' },      // 금색
    T2: { color: '#00AAFF', name: '커넥터' },    // 파랑
    T3: { color: '#00CC66', name: '액티브' },    // 초록
    T4: { color: '#888888', name: '일반' },      // 회색
    Ghost: { color: '#333333', name: '비활성' }  // 진회색
};
```

## ⚠️ 금지 사항

1. **Raw Data 저장 금지** - 변환된 벡터만 저장
2. **PII 수집 금지** - 개인 식별 정보 제외
3. **복잡한 추상화 금지** - 단순함 유지
4. **미사용 코드 금지** - 데드 코드 삭제

## 📊 주요 API 엔드포인트

### 인증
- `POST /auth/login` → JWT 토큰
- `POST /auth/api-key` → API 키 생성

### Physics
- `GET /physics/state` → 현재 상태
- `POST /physics/update` → 노드 업데이트
- `WebSocket /ws/physics-map` → 실시간

### AutoSync
- `POST /autosync/detect` → SaaS 자동 감지
- `POST /autosync/transform` → Zero Meaning 변환

### Metadata
- `POST /metadata/set` → 메타데이터 설정
- `GET /metadata/get/{node_id}` → 조회

## 🧪 테스트 가이드

```bash
# 전체 테스트
make test

# 특정 파일
pytest tests/test_api.py -v

# 커버리지
make test-cov
```

## 🐳 Docker 사용

```bash
make docker-up    # 시작
make docker-down  # 종료
make docker-logs  # 로그
```

## 💡 Cursor AI 힌트

### 파일 생성 시
- `autus-unified/backend/` 내에 생성
- `__init__.py` 포함
- 타입 힌트 필수

### 코드 수정 시
- 기존 패턴 따르기
- `main.py`에 라우터 등록
- 테스트 추가

### 프론트엔드 수정 시
- 기존 CSS 변수 사용
- 5-Tier 색상 준수
- 한글 UI 유지

## 📚 참고 문서

- API 문서: http://localhost:8000/docs
- Physics 공식: docs/PHYSICS_EQUATIONS.md
- 아키텍처: docs/ARCHITECTURE.md

---

*"측정할 수 없으면 관리할 수 없다" - 피터 드러커*
*"단순함이 궁극의 정교함이다" - 스티브 잡스*
