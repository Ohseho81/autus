# AUTUS 백업/복구/암호화 정책

- 모든 주요 데이터(데이터, 로그, 설정, 워크플로우)는 `backups/` 폴더에 주기적으로 tar.gz로 백업
- 백업 파일은 필요시 openssl로 AES256 암호화 가능
- 복구 시 지정 파일을 tar로 해제
- 백업/복구/암호화 스크립트는 scripts/에 위치
- 백업 파일은 30일 이상 보관하지 않음(정책)
