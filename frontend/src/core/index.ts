/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * 🏛️ AUTUS CORE MODULE
 * Top-Down Genesis · Physics · Simulation · Afterimage
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * 모듈 순서 (불변):
 * 1. Physics (헌법)
 * 2. Afterimage (불변 기록)
 * 3. Genome (업무 생태계)
 * 4. Geo (공간 인과)
 * 5. Gravity (환경 상수)
 * 6. Simulation (관측 전용)
 * 
 * 원칙:
 * - core/는 UI를 모른다
 * - 모든 함수는 순수 함수
 * - 하위는 상위를 변경 불가
 */

// ─────────────────────────────────────────────────────────────────────────────
// 1️⃣ PHYSICS (Universal Physics - 헌법)
// ─────────────────────────────────────────────────────────────────────────────

export {
  // Constants
  PHYSICS_CONSTANTS,
  GATE_STATES,
  SCALE_LEVELS,
  PSI_MIN,
  PSI_MAX,
  PSI_CRITICAL,
  ENTROPY_ACCELERATION_THRESHOLD,
  THETA_DEFAULT,
  ALPHA_URBAN,
  BETA_DEFAULT,
  GAMMA_DEFAULT,
  
  // Types
  type GateState,
  type ScaleLevel,
  
  // Gate Functions
  determineGate,
  checkGate,
  isG1Triggered,
  isG2Triggered,
  isG3Triggered,
  isAnyGateTriggered,
  canTransition,
  getNextPossibleStates,
  type GateInput,
  type GateOutput
} from './physics';

// ─────────────────────────────────────────────────────────────────────────────
// 2️⃣ AFTERIMAGE (Append-only Ledger)
// ─────────────────────────────────────────────────────────────────────────────

export {
  AfterImageLedger,
  getLedger,
  canReplay,
  verifyReplayChain,
  type AfterimageRecord,
  type AfterimageInput
} from './afterimage';

// ─────────────────────────────────────────────────────────────────────────────
// 3️⃣ GENOME (Work Genome Engine)
// ─────────────────────────────────────────────────────────────────────────────

export {
  evolveGenome,
  evolveState,
  shouldExtinct,
  shouldProliferate,
  proliferate,
  createGenome,
  createInitialState,
  type WorkGenome,
  type GenomeState,
  type EvolutionInput
} from './genome';

// ─────────────────────────────────────────────────────────────────────────────
// 4️⃣ GEO (Geo-Causal Kernel)
// ─────────────────────────────────────────────────────────────────────────────

export {
  haversineDistance,
  isPointInPolygon,
  getCrossedBoundaries,
  calculateDensity,
  propagate,
  propagateToAll,
  isValidCoordinate,
  isValidGeoNode,
  type GeoCoordinate,
  type GeoNode,
  type Boundary,
  type PropagationParams,
  type PropagationResult
} from './geo';

// ─────────────────────────────────────────────────────────────────────────────
// 5️⃣ GRAVITY (Environment Presets)
// ─────────────────────────────────────────────────────────────────────────────

export {
  REGION_PRESETS,
  getPreset,
  resolveGravity,
  calculateCost,
  canPassGate,
  getAllPresets,
  getPresetIds,
  type GravityPreset,
  type GravityContext,
  type ResolvedGravity
} from './gravity';

// ─────────────────────────────────────────────────────────────────────────────
// 6️⃣ SIMULATION (Observational Only)
// ─────────────────────────────────────────────────────────────────────────────

export {
  simulate,
  simulateSteps,
  createSimNode,
  createWorldState,
  type WorldState,
  type SimNode,
  type SimFrame,
  type SimResult
} from './simulation';
