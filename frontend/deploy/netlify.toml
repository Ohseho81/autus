# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 AUTUS - Netlify 배포 설정
# ═══════════════════════════════════════════════════════════════════════════════
# 
# autus-ai.com 배포
#
# ═══════════════════════════════════════════════════════════════════════════════

[build]
  publish = "."
  command = "echo 'Static site ready'"

[build.environment]
  NODE_VERSION = "20"

# SPA 라우팅
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Role = ["admin"]}

# 캐시 헤더 (정적 에셋)
[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=3600"

[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# 보안 헤더 + Supabase CSP
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://cdn.chart.js; connect-src 'self' https://pphzvnaedmzcvpxjulti.supabase.co wss://pphzvnaedmzcvpxjulti.supabase.co https://api.autus-ai.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; img-src 'self' data: blob:;"

# API 프록시 (Vercel API)
[[redirects]]
  from = "/api/*"
  to = "https://api.autus-ai.com/api/:splat"
  status = 200
  force = true
