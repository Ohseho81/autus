# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 AUTUS - Netlify 배포 설정
# ═══════════════════════════════════════════════════════════════════════════════
# 
# autus-ai.com 배포
#
# ═══════════════════════════════════════════════════════════════════════════════

[build]
  publish = "."
  command = "echo 'Static site ready'"

[build.environment]
  NODE_VERSION = "20"

# SPA 라우팅
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Role = ["admin"]}

# 캐시 헤더
[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# 보안 헤더
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# API 프록시 (Vercel API)
[[redirects]]
  from = "/api/*"
  to = "https://vercel-api-ohsehos-projects.vercel.app/api/:splat"
  status = 200
  force = true
