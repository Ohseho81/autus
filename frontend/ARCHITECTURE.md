# AUTUS Frontend Architecture

## 📁 디렉토리 구조

```
frontend/
│
├── index.js                 # 🎯 통합 진입점 (모든 모듈 export)
├── index.html               # 메인 HTML
├── autus-flywheel.html      # Flywheel OS UI
├── autus-system.js          # 통합 시스템 API
├── manifest.json            # PWA manifest
├── sw.js                    # Service Worker
│
├── core/                    # 📦 핵심 물리 엔진
│   ├── index.js             # Core 모듈 통합
│   ├── PhysicsKernel.js     # Holy Trinity Engine (관성/작용반작용/에너지보전)
│   ├── AccessRequester.js   # Reference Logic (임시 데이터 접근)
│   ├── DataBridge.js        # Persistent Access (데이터 브릿지)
│   ├── ClientProcessor.js   # Resource Offloading (클라이언트 처리)
│   ├── PackManager.js       # 팩 로딩/관리
│   ├── LearningEngine.js    # 패턴 학습
│   └── state.json           # Master State Definition
│
├── engine/                  # 🔧 비즈니스 로직 엔진
│   ├── index.js             # Engine 모듈 통합
│   ├── PhysicsMap.js        # Elon Engine (노드 시뮬레이션)
│   ├── converter.js         # Raw-to-Vector (데이터 변환)
│   ├── ValueInferenceEngine.js  # n² Network Value
│   └── PatternExtractor.js  # 익명화 패턴 추출
│
├── rendering/               # 🎨 Three.js 렌더링
│   └── index.js             # 렌더링 모듈 통합 (→ js/core)
│
├── api/                     # 🌐 백엔드 통신
│   └── index.js             # API 클라이언트 (→ js/api)
│
├── js/                      # 📜 레거시 JavaScript
│   ├── core/                # Three.js 레이어
│   │   ├── CoreLayer.js     # 중앙 코어 시각화
│   │   ├── GraphLayer.js    # 관계 그래프 시각화
│   │   ├── FlowLayer.js     # 에너지 흐름 시각화
│   │   ├── StateUniform.js  # 상태→셰이더 매핑
│   │   ├── DeterminismSampler.js  # 결정론적 난수
│   │   ├── AutusRenderer.js # 통합 렌더러
│   │   ├── PostProcessing.js # 포스트 프로세싱
│   │   └── ParticleTrail.js # 파티클 효과
│   ├── api/
│   │   └── AutusEngine.js   # API 클라이언트
│   └── physics-map.js       # 물리 맵 시각화
│
├── packs/                   # 📦 자동화 팩
│   ├── automation/          # 업무 자동화
│   │   ├── index.js
│   │   ├── observers/       # 3-Way Observer
│   │   │   ├── WebObserver.js
│   │   │   ├── DataObserver.js
│   │   │   ├── CommObserver.js
│   │   │   └── PatternMatcher.js
│   │   └── agents/
│   │       ├── ReportAgent.js
│   │       └── MemberManager.js
│   ├── education/           # 교육 자동화
│   │   ├── index.js
│   │   ├── StudentCore.js
│   │   ├── FeedbackAgent.js
│   │   └── ...
│   └── professional/        # 전문가 자동화
│       └── index.js
│
├── assets/                  # 정적 자원
├── css/                     # 스타일시트
├── ts/                      # TypeScript 소스
└── archive/                 # 아카이브
    └── legacy-html/         # 레거시 HTML 파일
```

## 🔄 모듈 관계도

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              AUTUS FRONTEND                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                        index.js (통합 진입점)                         │   │
│  └────────────────────────────────┬─────────────────────────────────────┘   │
│                                   │                                          │
│         ┌─────────────────────────┼─────────────────────────┐               │
│         │                         │                         │               │
│         ▼                         ▼                         ▼               │
│  ┌─────────────┐          ┌─────────────┐          ┌─────────────┐         │
│  │    core/    │          │   engine/   │          │  rendering/ │         │
│  │ (Physics)   │◀────────▶│ (Business)  │◀────────▶│  (Three.js) │         │
│  └──────┬──────┘          └──────┬──────┘          └──────┬──────┘         │
│         │                        │                        │                 │
│  ┌──────┴──────┐          ┌──────┴──────┐          ┌──────┴──────┐         │
│  │PhysicsKernel│          │PhysicsMap   │          │AutusRenderer│         │
│  │DataBridge   │          │Converter    │          │CoreLayer    │         │
│  │Accessor     │          │ValueInfer   │          │GraphLayer   │         │
│  │Processor    │          │PatternExt   │          │FlowLayer    │         │
│  └─────────────┘          └─────────────┘          └─────────────┘         │
│         │                        │                        │                 │
│         └────────────────────────┼────────────────────────┘                 │
│                                  │                                          │
│                                  ▼                                          │
│                         ┌─────────────┐                                     │
│                         │   packs/    │                                     │
│                         │(Automation) │                                     │
│                         └──────┬──────┘                                     │
│                                │                                            │
│              ┌─────────────────┼─────────────────┐                          │
│              │                 │                 │                          │
│              ▼                 ▼                 ▼                          │
│       ┌───────────┐     ┌───────────┐     ┌───────────┐                    │
│       │automation │     │ education │     │professional│                   │
│       │ 3-Way Obs │     │ StudentCore│    │ AutoReport │                   │
│       │ Matcher   │     │ Feedback  │     │ RelSync    │                   │
│       └───────────┘     └───────────┘     └───────────┘                    │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

## 📊 데이터 흐름

```
Raw Data (User Device)
        │
        ▼
┌───────────────┐     ┌───────────────┐
│  DataBridge   │────▶│AccessRequester│
│  (Persistent) │     │  (Temporary)  │
└───────────────┘     └───────┬───────┘
                              │
                              ▼
                    ┌───────────────┐
                    │   Converter   │
                    │(Raw→Physics)  │
                    └───────┬───────┘
                            │ ⚠️ DESTROY RAW DATA
                            ▼
                    ┌───────────────┐
                    │  PhysicsMap   │
                    │(Elon Engine)  │
                    └───────┬───────┘
                            │
              ┌─────────────┼─────────────┐
              │             │             │
              ▼             ▼             ▼
        ┌──────────┐ ┌───────────┐ ┌──────────┐
        │Observers │ │ValueInfer │ │ Renderer │
        │(Pattern) │ │(n² Value) │ │(Three.js)│
        └────┬─────┘ └─────┬─────┘ └──────────┘
             │             │
             ▼             ▼
        ┌───────────────────────┐
        │   PatternExtractor    │
        │ (Anonymize for Server)│
        └───────────┬───────────┘
                    │
                    ▼ Physics ONLY (No PII)
              ┌───────────┐
              │  SERVER   │
              └───────────┘
```

## 🚀 사용 예시

### 1. 기본 초기화

```javascript
import { initAutus } from './index.js';

const system = await initAutus({
    onOpportunityFound: (opportunity) => {
        console.log('Automation opportunity:', opportunity);
    }
});

system.start();
```

### 2. 특정 모듈만 사용

```javascript
import { PhysicsKernel, PhysicsMap } from './index.js';

// Physics 계산
const inertia = PhysicsKernel.calculateInertia({ mass: 5, friction: 0.3 });

// 노드 이동
PhysicsMap.moveNode('User', { x: 10, y: 5, z: 0 });
const prediction = PhysicsMap.predictActionChange(PhysicsMap.getUserNode());
```

### 3. 자동화 팩 로드

```javascript
import { loadPack } from './index.js';

const educationPack = await loadPack('education');
const result = educationPack.StudentCore.analyzeStudentPerformance(testResults);
```

### 4. Three.js 렌더링

```javascript
import { initRenderer } from './index.js';

const renderer = await initRenderer(document.getElementById('canvas'), state);
renderer.updateState(newState);
```

## 📋 핵심 원칙

1. **No Raw Data Storage**: 서버는 물리 속성만 저장
2. **Client-Side Processing**: 무거운 연산은 클라이언트에서
3. **Physics Only**: 모든 데이터는 물리 속성으로 변환
4. **Deterministic**: 동일 입력 → 동일 출력
5. **Privacy First**: PII는 절대 서버로 전송 안 함




