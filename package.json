{
  "name": "autus",
  "version": "1.0.0",
  "description": "AUTUS — Decision Physics System",
  "type": "module",
  "scripts": {
    "capture:golden": "npx tsx ui/scripts/capture-golden.ts",
    "test:visual": "playwright test --config=ui/playwright.config.ts",
    "test:visual:update": "playwright test --config=ui/playwright.config.ts --update-snapshots",
    "test:visual:report": "playwright show-report playwright-report",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0",
    "tsx": "^4.7.0",
    "typescript": "^5.0.0"
  }
}
