{
  // ═══════════════════════════════════════════════════════════════
  // 🔧 AUTUS - 태스크 설정
  // ═══════════════════════════════════════════════════════════════
  "version": "2.0.0",
  "tasks": [
    // ─────────────────────────────────────────────────────────────
    // 🚀 서버 실행
    // ─────────────────────────────────────────────────────────────
    {
      "label": "🚀 Start Backend API",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified/backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
      "group": "build",
      "presentation": {
        "reveal": "always",
        "panel": "new",
        "focus": true
      },
      "problemMatcher": [],
      "isBackground": true
    },
    {
      "label": "🌐 Start Frontend Server",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified/frontend && python3 -m http.server 3000",
      "group": "build",
      "presentation": {
        "reveal": "always",
        "panel": "new"
      },
      "problemMatcher": [],
      "isBackground": true
    },
    {
      "label": "🔥 Start Full Stack",
      "dependsOn": [
        "🚀 Start Backend API",
        "🌐 Start Frontend Server"
      ],
      "dependsOrder": "parallel",
      "problemMatcher": []
    },
    
    // ─────────────────────────────────────────────────────────────
    // 🧪 테스트
    // ─────────────────────────────────────────────────────────────
    {
      "label": "🧪 Run All Tests",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && python -m pytest tests/ -v --tb=short",
      "group": "test",
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": []
    },
    {
      "label": "🧪 Run Tests with Coverage",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && python -m pytest tests/ -v --cov=backend --cov-report=html",
      "group": "test",
      "presentation": {
        "reveal": "always"
      },
      "problemMatcher": []
    },
    
    // ─────────────────────────────────────────────────────────────
    // 📦 의존성 관리
    // ─────────────────────────────────────────────────────────────
    {
      "label": "📦 Install Dependencies",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && pip install -r backend/requirements.txt",
      "group": "build",
      "presentation": {
        "reveal": "always"
      },
      "problemMatcher": []
    },
    {
      "label": "📦 Create Virtual Environment",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && python3 -m venv venv && source venv/bin/activate && pip install -r backend/requirements.txt",
      "group": "build",
      "problemMatcher": []
    },
    
    // ─────────────────────────────────────────────────────────────
    // 🔍 코드 품질
    // ─────────────────────────────────────────────────────────────
    {
      "label": "🔍 Lint (Ruff)",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && ruff check backend/",
      "group": "test",
      "presentation": {
        "reveal": "always"
      },
      "problemMatcher": []
    },
    {
      "label": "🔧 Format (Ruff)",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && ruff format backend/",
      "group": "build",
      "problemMatcher": []
    },
    {
      "label": "🔧 Fix All (Ruff)",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && ruff check --fix backend/ && ruff format backend/",
      "group": "build",
      "problemMatcher": []
    },
    
    // ─────────────────────────────────────────────────────────────
    // 🐳 Docker
    // ─────────────────────────────────────────────────────────────
    {
      "label": "🐳 Docker Compose Up",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && docker-compose up -d",
      "group": "build",
      "problemMatcher": []
    },
    {
      "label": "🐳 Docker Compose Down",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && docker-compose down",
      "group": "build",
      "problemMatcher": []
    },
    {
      "label": "🐳 Docker Build",
      "type": "shell",
      "command": "cd ${workspaceFolder}/autus-unified && docker-compose build",
      "group": "build",
      "problemMatcher": []
    },
    
    // ─────────────────────────────────────────────────────────────
    // 🔄 Git
    // ─────────────────────────────────────────────────────────────
    {
      "label": "📊 Git Status",
      "type": "shell",
      "command": "git status",
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": []
    },
    
    // ─────────────────────────────────────────────────────────────
    // 🧹 정리
    // ─────────────────────────────────────────────────────────────
    {
      "label": "🧹 Clean Pycache",
      "type": "shell",
      "command": "find ${workspaceFolder} -type d -name '__pycache__' -exec rm -rf {} + 2>/dev/null; find ${workspaceFolder} -type f -name '*.pyc' -delete 2>/dev/null; echo '✅ Cleaned!'",
      "problemMatcher": []
    }
  ]
}
